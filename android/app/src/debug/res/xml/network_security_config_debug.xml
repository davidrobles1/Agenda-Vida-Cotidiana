<?xml version="1.0" encoding="utf-8"?>
<!--
  AND-002/AND-003: cleartext HTTP is only ever needed to reach the local dev
  backend/Keycloak during development (emulator alias 10.0.2.2, or the Mac's
  LAN IP for a physical device) — never in a release build, where this file
  is not merged in at all (debug-only source set).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
