# Vida Cotidiana — Android (AND-001/AND-002/AND-003)

Stack (08-android-architecture.md): Kotlin, Jetpack Compose, Hilt, Coroutines/Flow,
Navigation Compose, Retrofit/OkHttp, Kotlin Serialization. `minSdk = 30` (DEC-011).

**ASSUMPTION:** exact AGP/Kotlin/Compose BOM/Hilt versions (AGP 8.5.2, Kotlin 2.0.20,
Compose BOM 2024.09.00, Hilt 2.51.1, Gradle 8.9) are a technical bootstrap choice, not
an approved business decision — resolves the "versiones exactas TBD al crear el
proyecto" note in `17-dependencies.md`.

## AND-002 — login (real, 2026-08-15)

AppAuth-Android (`net.openid:appauth`), Authorization Code + PKCE (S256, generated by
AppAuth itself) against the `android-app` Keycloak client (Phase 1, public client, no
client secret). Login opens Custom Tabs, never a WebView (AppAuth's own security
guidance). Tokens are stored in `EncryptedSharedPreferences`
(`core/security/TokenStore.kt`, AES256-GCM, key material in the Android Keystore).

Redirect: `com.vidacotidiana.app://callback`, wired via
`manifestPlaceholders["appAuthRedirectScheme"]` in `app/build.gradle.kts` — **verified
in the built APK's actual merged manifest** (`aapt dump xmltree`), not just assumed
from the Gradle config: `RedirectUriReceiverActivity` is registered for an
`android.intent.action.VIEW` intent-filter on scheme `com.vidacotidiana.app`.

## AND-003 — reminders (real, 2026-08-15)

Retrofit + OkHttp + kotlinx.serialization (`com.jakewharton.retrofit:retrofit2-kotlinx-serialization-converter`),
an OkHttp interceptor attaches `Authorization: Bearer <token>` from `TokenStore`.
`RemindersScreen`/`RemindersViewModel`: list (`GET /reminders`), create
(`POST /reminders`), complete (`POST /reminders/{id}/complete`).

Token refresh-on-401 is **not implemented** in this bootstrap (ASSUMPTION, not a
decision — ties into token-lifecycle hardening, out of scope for this first vertical
slice; the access token is short-lived, 5 min, same as every other client in the realm).

## Build status (real, 2026-08-15)

Android SDK installed via terminal only (no Android Studio): `cmdline-tools` downloaded
directly, `sdkmanager "platform-tools" "platforms;android-34" "build-tools;34.0.0"`,
license accepted non-interactively. `./gradlew clean assembleDebug`: **BUILD
SUCCESSFUL**, real APK produced at `app/build/outputs/apk/debug/app-debug.apk` (~36 MB).

**UI-level testing (actually tapping through login/reminders) is NOT_EXECUTED —
BLOCKED_BY_ENVIRONMENT:** `adb devices` lists nothing; no Android emulator was set up
(only the packages explicitly requested — `platform-tools`, a platform, build-tools —
were installed, deliberately not the `emulator`/system-image packages, to keep disk
footprint minimal and per the "no Android Studio" instruction). The APK's manifest was
independently verified with `aapt dump xmltree` to confirm the AppAuth redirect wiring
is real, but nobody has tapped the actual login button on a device.

### Installing on a real device

```
adb install app/build/outputs/apk/debug/app-debug.apk
```

Requires USB debugging enabled on the phone and, the first time an unsigned/debug APK
from outside a store is installed, **"Install unknown apps" enabled for whichever app
you used to transfer it** (adb doesn't need this — a manual AirDrop/file-copy transfer
does).

### Backend/Keycloak host resolution

- **Emulator:** `10.0.2.2` (special alias to the host's `localhost`) — already the
  default in `app/build.gradle.kts` (`OIDC_ISSUER`, `API_BASE_URL`).
- **Physical device on the same LAN:** replace `10.0.2.2` with the Mac's real LAN IP in
  those two `buildConfigField` values, **and** add that same IP to
  `app/src/debug/res/xml/network_security_config_debug.xml` (cleartext HTTP is only
  allowed for the domains listed there, debug builds only — never in release).

## Structure

```
app/src/main/java/com/vidacotidiana/app/
  core/network/    — Retrofit API, DTOs, Hilt network module (AND-003)
  core/security/   — EncryptedSharedPreferences token store (AND-002)
  core/ui/         — empty, reserved
  feature/auth/    — AppAuth login screen + manager (AND-002)
  feature/reminders/ — reminders list/create/complete (AND-003)
  feature/{home,sharing,settings}/ — empty, reserved
  navigation/      — NavGraph (login → reminders)
```
