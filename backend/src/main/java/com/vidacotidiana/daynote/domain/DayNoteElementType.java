package com.vidacotidiana.daynote.domain;

/** Pedido explícito del usuario (2026-08-22): "El Canvas solo permitirá: 1
    tipo de forma: sticker/banner. 1 tipo de texto." Exactamente estos 2
    valores — sin más tipos, a propósito (mismo "no sobrearquitecturar"
    que el resto de este proyecto). */
public enum DayNoteElementType {
    BANNER,
    TEXT
}
