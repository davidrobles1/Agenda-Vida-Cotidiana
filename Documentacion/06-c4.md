# 06 — Arquitectura C4

**DECISION (ADR-005):** V1 cubre Android, iOS y Web. Actualizado desde el diagrama inicial que solo contemplaba Android.

## Contexto
```mermaid
C4Context
title Vida Cotidiana - System Context
Person(user, "Usuario", "Persona que usa la app en Android, iOS o Web")
System(app, "Vida Cotidiana", "Organización de vida cotidiana")
System_Ext(identity, "Keycloak (OIDC)", "Proveedor de identidad self-hosted, DEC-004/ADR-008")
System_Ext(push, "Firebase Cloud Messaging", "Proveedor de push unificado detrás de PushNotificationSender, DEC-010/ADR-007")
System_Ext(mail, "Proveedor de correo (TBD)", "Envío de invitaciones por email, DEC-009 reabierta — ver ADR-014/22-decision-log.md")
Rel(user, app, "Usa")
Rel(app, identity, "Autentica (Authorization Code + PKCE)")
Rel(app, push, "Registra dispositivo / recibe eventos")
Rel(app, mail, "Envía invitación por email")
```

## Contenedores
```mermaid
C4Container
title Vida Cotidiana - Containers
Person(user, "Usuario")
Container(android, "Android App", "Kotlin, Jetpack Compose", "UI y almacenamiento local")
Container(ios, "iOS App", "Swift, SwiftUI (DEC-006/ADR-010)", "UI y almacenamiento local")
Container(web, "Web App", "React, TypeScript, SPA (DEC-007/ADR-011)", "UI web")
Container(api, "Backend API", "Java 21, Spring Boot", "Monolito modular, hospedado en un servidor propio alquilado (DEC-008/ADR-014)")
ContainerDb(db, "PostgreSQL", "SQL", "Datos de aplicación, invitaciones y compartición")
ContainerDb(obj, "Object Storage", "S3 compatible", "Futuras cargas de archivos - FUTURE")
Rel(user, android, "Usa")
Rel(user, ios, "Usa")
Rel(user, web, "Usa")
Rel(android, api, "HTTPS/JSON")
Rel(ios, api, "HTTPS/JSON")
Rel(web, api, "HTTPS/JSON")
Rel(api, db, "SQL/TLS")
Rel(api, obj, "HTTPS")
```

## V1
- Object Storage queda preparado conceptualmente pero no se implementa (V1 no maneja archivos/adjuntos).
- iOS y Web comparten el mismo backend/API/modelo de datos que Android; no se asume código compartido entre plataformas (CLAUDE.md).
- Stack tecnológico definitivo de iOS y Web: **DECISION** (DEC-006/DEC-007) — ver `08b-ios-architecture.md` y `08c-web-architecture.md`.
- **Nota de seguridad no bloqueante:** al ser el cliente Web una SPA (DEC-007), el patrón de almacenamiento de tokens OIDC en el navegador queda como consideración técnica a resolver antes de implementar el cliente Web (ver `11-auth-security.md`); no es una decisión de negocio y no bloquea el resto de V1.
