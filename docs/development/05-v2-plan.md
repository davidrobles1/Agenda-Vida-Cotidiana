# 05 — V2 Plan (propuesta de alcance, no un cronograma)

**Qué es este documento:** una auditoría real de si V1 está de verdad completo, un catálogo honesto de trabajo que ya se hizo y que en la práctica es V2 (aunque nunca se declaró como tal), y una propuesta de alcance restante para V2 — todo marcado **TBD** para que el usuario priorice. No hay fechas ni compromisos aquí porque nadie los ha pedido todavía. No se implementa nada de la sección 3 en esta tarea.

---

## 1. Auditoría real: ¿V1 está completo? (`02-roadmap.md` líneas 12-21)

`02-roadmap.md` nunca fue verificado línea por línea contra el código real — era, literalmente, un placeholder desde la fase de kickoff. Esta es esa verificación, punto por punto, con evidencia (referencia a test/commit/doc) o gap declarado explícitamente.

| # | Criterio (`02-roadmap.md`) | Estado | Evidencia |
|---|---|---|---|
| 1 | Usuario puede registrarse/iniciar sesión en Android, iOS y Web | **ACTUALIZADO 2026-08-17 — PARTIAL, gap real solo en iOS** | Login: **DONE** (Android/Web: `LoginAndRemindersFlowTest`, `sharing.spec.ts`/`notifications.spec.ts`/`error-tracking.spec.ts`; iOS: `IOS-002`, `LoginAndRemindersUITests` en Simulador). **Registrarse (`AND-008`/`WEB-008`, `33-security-cross-audit.md` §1.2): CERRADO en Web y en Android, ambos con verificación real de punta a punta en su plataforma** (Web: cuenta `newuser_223zcs`; Android: cuenta `devicetest_37282` creada de cero en el teléfono físico real, sincronizada en Postgres). **iOS: sin ningún trabajo de registro** — `LoginScreen`/`AuthManager` de iOS no tienen equivalente, sigue siendo un gap real. |
| 2 | Puede crear y completar un recordatorio | **DONE** | Mismos tests de arriba cubren create+complete contra el backend real en las 3 plataformas (`AND-002/003`, `WEB-002/003`, `IOS-002/003`). |
| 3 | Puede compartir un recordatorio mediante invitación (aceptar/rechazar/ver/completar según permiso, ADR-006) | **PARTIAL — gap real en iOS** | Android/Web: `SharingFlowTest` + `sharing.spec.ts`, ambos contra el backend real, en verde. iOS: `IOS-004` (Sharing/Invitations UI) está **`TODO, deliberadamente pospuesto`** en `01-technical-backlog.md` — el código (`ShareView.swift`/`InvitationsView.swift`) existe pero nunca se verificó con un test real. El criterio dice "el usuario puede compartir", sin acotar a una plataforma, así que hoy no se cumple en las 3 — es un gap real, no una reetiquetación. |
| 4 | Recibe notificaciones **locales** y push para recordatorios propios y compartidos (ADR-007) | **CERRADO (Android/Web) — 2026-08-16, `AND-007`/`WEB-007`** | Gap real confirmado en la corrida anterior de esta auditoría (nada de código para `WorkManager`/`AlarmManager`/`UNUserNotificationCenter`/Notification API), cerrado en la misma sesión que lo encontró. Android: `AlarmManager.setExactAndAllowWhileIdle` real (`ReminderAlarmScheduler.kt`), permiso de alarmas exactas solicitado en runtime (API 31+), `BootRescheduleReceiver` re-arma tras reinicio (verificado con un reinicio real del dispositivo). Web: `setTimeout` real (`core/notifications/localReminderTimer.ts`), best-effort mientras la pestaña esté abierta — limitación de plataforma documentada explícitamente en `08c-web-architecture.md`, no forzada. Verificado en ambas plataformas con evidencia real (notificación real en la bandeja de Android, `new Notification(...)` real capturada en un test de Playwright para Web) — ver `01-technical-backlog.md` (`AND-007`/`WEB-007`) para el detalle completo, incluyendo dos bugs reales encontrados en el camino (permiso `POST_NOTIFICATIONS` nunca solicitado en runtime desde `AND-005` — afecta también al push existente, no solo a esto; y datetime-local con precisión de minuto en el test de Web). iOS sigue sin esto (desarrollo pausado). |
| 5 | Datos aislados por usuario y por relación de compartición explícita (owner vs. colaborador) | **DONE** | `ReminderControllerIntegrationTest`: `getReminder_ownedByAnotherUserReturnsNotFound_neverForbidden`, `updateReminder_ownedByAnotherUserReturnsNotFound_neverForbidden`, `deleteReminder_ownedByAnotherUserReturnsNotFound_neverForbidden` — diseño deliberado de 404 (nunca 403) para no revelar existencia (`SEC-001`, `11-auth-security.md`). |
| 6 | Aplicación maneja errores de red | **DONE (nivel básico, no auditado exhaustivamente)** | Android: manejo real (`try`/`catch`/`runCatching`) en los 5 `ViewModel`/pantalla que hacen red (`RemindersViewModel`, `InvitationsViewModel`, `NotificationsViewModel`, `ShareDialog`, `LoginScreen`), con estado de error visible en UI (verificado visualmente en `UX-004`). Web: mismo patrón (`try`/`catch` en cada llamada a `core/*/api.ts`, `role="alert"` en pantalla). iOS: no auditado en esta pasada (desarrollo pausado). No se verificó cobertura exhaustiva (timeouts específicos, reintentos, cola offline) — solo que existe manejo real, no una promesa vacía. |
| 7 | Backend tiene pruebas automatizadas | **DONE** | `./mvnw clean test`: **110/110 en verde**, reconfirmado ahora mismo (no un número heredado sin verificar) — Testcontainers (Postgres real), unitarios + integración + contract tests (`TEST-API-001`) contra `openapi.yaml` real. |
| 8 | CI bloquea cambios que fallen compilación/pruebas | **GAP REAL, CONFIRMADO — no se cumple hoy** | `.github/workflows/backend-ci.yml` existe (`INFRA-003`, marcado DONE en el backlog) pero **no hay ningún remoto de git configurado** (`git remote -v` vacío) — el repo nunca se subió a GitHub, así que ese workflow **nunca se ha ejecutado ni una sola vez**. "DONE" en el backlog describe que el YAML fue escrito correctamente, no que esté bloqueando nada. El criterio del roadmap ("CI bloquea cambios") es falso hoy, tal como el usuario sospechaba — no se le da el beneficio de la duda por existir el archivo. |
| 9 | No existen secretos en repositorio | **VERIFICADO DE VERDAD — 2026-08-16, `33-security-cross-audit.md` §5** | `gitleaks detect --source . --log-opts="--all"` corrido de verdad contra los 17 commits del historial (no solo el HEAD). 3 hallazgos: 1 real (token de API de GlitchTip hardcodeado en un test, corregido — movido a `.env`), 2 falsos positivos (API keys públicas de Firebase, confirmado contra la documentación oficial de Google que no son secretas por diseño). El commit histórico donde se introdujo el hallazgo real sigue teniendo el valor viejo — no se reescribió el historial sin autorización explícita. |
| 10 | OpenAPI está versionado | **DONE** | `Documentacion/openapi/openapi.yaml`, `info.version: 1.0.0`, bajo control de versiones, validado contra respuestas reales (`TEST-API-001`, encontró y corrigió `BE-035`). |

**Resumen (actualizado 2026-08-17, tras verificar en el dispositivo físico real):** 8/10 `DONE` con evidencia real. 1/10 `PARTIAL` en dos criterios distintos (punto 1: registro — cerrado en Web y Android con verificación real de punta a punta, gap real todavía en iOS; punto 3: compartir — falta iOS). 1/10 con **gap real confirmado, todavía abierto** (CI real, punto 8 — depende de tener un repo remoto).

**Hallazgos adicionales de la auditoría de seguridad (`33-security-cross-audit.md`), fuera de los 10 criterios del roadmap pero relevantes para "V1 completo":** `bruteForceProtected`/`registrationAllowed` — **ambos cerrados 2026-08-16/17** (`33-security-cross-audit.md` §1.1/§1.2). `resetPasswordAllowed: false` sigue sin cerrar — depende de DEC-009 (proveedor de correo), todavía `TBD` (ver nota actualizada de DEC-009 en `22-decision-log.md`).

**Verificación en dispositivo físico real, 2026-08-17 (el usuario conectó el teléfono y pidió continuar):** `UX-006` (restilo Android), `AND-008` (registro real) y `AND-009` (refresh de sesión) — los 3 verificados de punta a punta contra hardware real, no solo build/emulador. Encontrados y corregidos 2 bugs reales de navegación que **solo se manifestaban en el dispositivo físico** (build/lint/e2e de escritorio no los detectan): post-login aterrizaba en `Routes.HOME` en vez de `Routes.REMINDERS` (rompía los 3 tests instrumentados reales), y un `startDestination` sin `remember{}` en `AppNavGraph` que se recalculaba en cada recomposición y pisaba la navegación real justo después del login. Detalle completo en `01-technical-backlog.md` (fila `UX-006`).

**V1_READINESS conclusión (actualizada):** V1 sigue sin estar 100% cerrado bajo su propia definición de éxito. Lo que queda: recuperación de cuenta (bloqueada por DEC-009), CI real (bloqueado por no tener repo remoto), sharing/registro en iOS. Esa decisión de qué se cierra como continuación de V1 vs. se declara "suficientemente completo" sigue siendo del usuario, no se toma aquí.

---

## 2. Catálogo: trabajo ya hecho que es, en la práctica, V2

Ninguno de estos ítems fue declarado formalmente como "V2" cuando se hizo — se hicieron inmediatamente después de estabilizar el vertical slice base, sin pausa ni anuncio de versión. Esta es la reconstrucción honesta de qué se hizo, cuándo, y bajo qué ID — no una re-etiquetación silenciosa.

| Fecha | Backlog ID | Qué | Encaja en V2 porque... |
|---|---|---|---|
| 2026-08-15 | `DEVOPS-001` | Rate limiting (429) sobre creación de invitaciones | **Nota honesta:** esto cierra `SEC-001`, un requisito de seguridad ya documentado como línea base de V1 en `11-auth-security.md` — no es funcionalidad nueva de V2, es V1 cerrando su propio requisito, entregado en una sesión posterior al vertical slice original. Se incluye aquí porque el usuario lo nombró como candidato, pero la clasificación correcta probablemente sea "V1, entregado tarde" — no "V2 adelantado". |
| 2026-08-15 | `BE-029` | Auditoría de eventos de seguridad (creación/aceptación/rechazo/cancelación/revocación/expiración) | Misma nota que arriba: cierra la sección "Auditoría" de `11-auth-security.md`, ya comprometida en V1. |
| 2026-08-15 | `DEVOPS-002` | SAST local (SpotBugs, `./mvnw spotbugs:check` manual — sin CI) | Zona gris: apoya `14-definition-of-done.md` pero no es uno de los 10 criterios explícitos de V1. Ejecución manual únicamente — mismo patrón de gap que el CI real (punto 8 arriba). |
| 2026-08-15 | `TEST-API-001` | Contract tests contra `openapi.yaml` real (encontró y corrigió `BE-035`) | Refuerza el criterio 7/10 y 10/10 de arriba (pruebas automatizadas + OpenAPI versionado y ahora validado), más que ser un ítem nuevo de V2 por sí solo. |
| 2026-08-16 | `AND-006`/`WEB-006`/`INFRA-007` | Reporte de errores: Firebase Crashlytics (Android) + GlitchTip autoalojado (Web) | **Esto sí es V2 sin ambigüedad** — observabilidad nueva, no estaba en ninguno de los 10 criterios de V1, coincide directamente con la definición de V2 de `CLAUDE.md` ("observabilidad"). Explícitamente **solo desarrollo local** — no conectado a `staging`/producción (depende de `INFRA-004`, todavía `TODO`) — declarado así en el propio backlog, no ocultado. |
| 2026-08-16 | `INFRA-006` | `/actuator/health` verificado y endurecido (caso UP y DOWN reales) | V2 sin ambigüedad — observabilidad, no era parte de los 10 criterios de V1. |
| 2026-08-16 | `UX-001`..`UX-005` | Sistema de diseño (`Documentacion/02-ux-ui/`, gap real nunca cerrado desde el kickoff) + restilo de Android/Web + tema de login de Keycloak | V2 sin ambigüedad — coincide directamente con "UX" en la definición de V2 de `CLAUDE.md`. Cubre las 5 pantallas de Android y las 5 de Web (`CallbackPage.tsx` excluida, es un passthrough sin UI visible) — **iOS no recibió nada de esto**, sigue con el estilo por defecto de SwiftUI. |
| 2026-08-16 | `UX-006` | Rediseño dashboard (imágenes de referencia del usuario) — tema oscuro real corregido con muestreo de píxel, librería de componentes de dashboard, Home con datos reales, 6 módulos nuevos de solo-maqueta (Documentos/Inventario/Garantías/Mantenimiento/Suscripciones/Familia) | V2 sin ambigüedad — misma categoría "UX" que `UX-001`..`UX-005`. **iOS tampoco recibió nada de esto.** Finanzas/IA Asistente explícitamente ausentes (restricción `CLAUDE.md`). Detalle completo en `01-technical-backlog.md`/`design-system.md` §7. |
| 2026-08-17 | `UX-007` | Home restilado a "Agenda" (jerarquía Hoy/Próximos días antes que métricas), fondo tipo libreta (Home/Calendario), `MetricCard` compacto, pantalla nueva de Calendario (grid de mes + lista "Pendientes" con checkbox, real+mock) | V2 sin ambigüedad — misma categoría "UX" que `UX-001`..`UX-006`. **iOS tampoco recibió nada de esto.** Cero backend nuevo — Calendario agrega client-side dos endpoints ya existentes (`GET /reminders`, `GET /me/invitations`) más `MockData`. Detalle completo en `01-technical-backlog.md`/`design-system.md` §8. |
| 2026-08-17 | `UX-009`/`UX-010` | Home: ilustración SVG + 3ª métrica/actividad reciente (mock) + accesos rápidos (real). Calendario: selección de día, panel de detalle, creación rápida real, estados pendiente/vencida/completada reales en el grid, responsive real | V2 sin ambigüedad — misma categoría "UX". Cero backend nuevo en ambos. Detalle completo en `01-technical-backlog.md`/`design-system.md`. |
| 2026-08-17 | `UX-008` | Identidad "Agenda" (beige + azul + terracota, tipografía Fraunces) generalizada de `LoginPage.tsx` a toda la plataforma Web — paleta centralizada en `index.css`, migración de `LoginPage.module.css` al sistema de tokens compartido | V2 sin ambigüedad — misma categoría "UX". **Alcance explícito: solo Web.** Android/iOS no se tocaron, pospuestos por decisión del usuario. Detalle completo en `01-technical-backlog.md`/`design-system.md` §9. |
| 2026-08-18 | `BE-037`/`WEB-009` | Bloque B.1: Garantías y Mantenimiento reales (`FR-020`) — antes 100% mock. Backend Clean Architecture completo (mismo patrón que `reminder/`), estado `COMPLETADO` real y persistido (reemplaza el toggle local-only que se perdía al recargar), Web migrado a datos reales | V2 sin ambigüedad. Trabajo en paralelo con Bloques A/B.2/C del mismo ciclo — ver nota de alcance en paralelo abajo. Detalle completo en `01-technical-backlog.md`/`09-data-model.md`. |
| 2026-08-18 | `BE-038` | ADR-015 Bloque B.2: `USER.personal_enabled`/`laboral_enabled`, `REMINDER.context`, `POST /me/modes` real — incluye la migración de backfill real (`V6__adr015_context_modes.sql`, TBD resuelto explícitamente por el Product Owner: PERSONAL + grandfather, verificado contra la base de datos de desarrollo real) | V2 sin ambigüedad (parte de ADR-015, adelantada en paralelo con V2). Trabajo en paralelo con Bloques A (`UX-013`, Semana/Día), B.1 (`BE-037`/`WEB-009`, Garantías/Mantenimiento) y C (`UX-012`, navegación Personal/Laboral) — coordinado solo en el contrato exacto de `POST /me/modes`, comunicado de antemano al Bloque C. Detalle completo en `01-technical-backlog.md`/`09-data-model.md`/`22-decision-log.md` (ADR-015). |
| 2026-08-17/18 | `UX-011` | Adopción de React Aria Components + Motion, elegida por el usuario tras dos rondas de evaluación comparativa — Fase 0 (fundación: `core/motion/tokens.ts`, `MotionConfig` global) + Fase 1 (Calendario reescrito sobre `<Calendar>` de React Aria: navegación real por teclado, transición espacial de mes con swipe en móvil, continuidad día→panel) + Fase 2 (`ShareDialog` de panel inline a `DialogTrigger`+`Popover`+`Dialog` real: focus trap, cierre con Escape, retorno de foco, integración real Motion+React Aria vía `motion.create`) + Fase 3 (filtro de Inventario → `ToggleButtonGroup` real, navegación por flechas) + Fase 4 (confirmación real antes de revocar un colaborador activo, `Modal`+`Dialog role="alertdialog"` — 2 bugs reales de integración Motion+React Aria encontrados y corregidos durante la verificación visual) | V2 sin ambigüedad — misma categoría "UX". **Alcance explícito: Calendario + ShareDialog + Inventario, solo Web** — el resto de módulos queda para fases siguientes, pedidas explícitamente "módulo por módulo". Fases 3 y 4 evaluadas para ejecución en paralelo por pedido explícito del usuario — determinado que el desarrollo era seguro en paralelo (archivos completamente disjuntos: `FilterChip.tsx`/`InventoryPage.tsx` vs `ShareDialog.tsx`) pero la verificación e2e debía ser secuencial (backend/Keycloak real compartido, mismo límite de serialización a 1 worker ya documentado por la protección de fuerza bruta). Detalle completo en `01-technical-backlog.md`/`design-system.md` §10. |
| 2026-08-18 | `UX-013` | Bloque A: vistas Semana/Día del Calendario — pill selector Día/Semana/Mes, timeline de 06:00-22:00, `DayAgenda.tsx` extraído y reutilizado por Mes/Semana/Día, `useCalendarData()` sin cambios | V2 sin ambigüedad. Trabajo en paralelo con Bloques B.1/B.2/C del mismo ciclo — ver nota de alcance en paralelo abajo. Detalle completo en `01-technical-backlog.md`. |
| 2026-08-18 | `UX-012` | Bloque C: navegación Personal/Laboral (ADR-015) — onboarding real, selector superior condicional, navbar de 4 ítems por modo, Ajustes para activar el modo restante, tema Laboral (paleta WCAG-verificada) | V2 sin ambigüedad (parte de ADR-015, adelantada en paralelo con V2). Trabajo en paralelo con Bloques A/B.1/B.2 del mismo ciclo. **Único de los 3 bloques paralelos cuyo agente fue cortado por el límite de sesión antes de verificación e2e en vivo** — verificado real recién en la integración a un solo árbol (`e2e/mode-navigation.spec.ts`, 19/19 de la suite completa en verde). Detalle completo en `01-technical-backlog.md`/`design-system.md` §11. |

---

## 3. Alcance restante propuesto para V2 — TBD, para que el usuario priorice

Basado únicamente en lo que `CLAUDE.md` ya define para V2 ("seguridad adicional, UX, calidad, observabilidad, correcciones y mejoras necesarias derivadas de V1") y en gaps reales encontrados en este mismo proyecto (sección 1). Nada de esto es funcionalidad nueva inventada — es continuación de lo que ya existe o cierre de lo que falta. **Nada aquí está decidido ni tiene fecha.**

- ~~Notificaciones locales~~ — **cerrado** (`AND-007`/`WEB-007`, 2026-08-16). Queda registrado en la sección 1, no como pendiente.
- ~~Auditoría cruzada de seguridad~~ — **corrida** (`33-security-cross-audit.md`, 2026-08-16).
- ~~Protección contra fuerza bruta~~ — **cerrado** (`33-security-cross-audit.md` §1.1, 2026-08-17), verificado con intentos de login reales, defaults de Keycloak sin inventar umbrales.
- ~~Autorregistro real~~ — **cerrado en Web con verificación real de punta a punta; código real en Android sin verificar en dispositivo** (`AND-008`/`WEB-008`, `33-security-cross-audit.md` §1.2, 2026-08-17). iOS sigue sin ningún trabajo de registro.
- ~~Renovación silenciosa de token en Android~~ — **mecanismo cerrado** (`AND-009`, `33-security-cross-audit.md` §1.3, 2026-08-17): `AuthState`/`performActionWithFreshTokens`/`TokenAuthenticator` reales, refresh de servidor confirmado con una petición real. Verificación de punta a punta en dispositivo físico queda pendiente (ver abajo).
- **TBD — Recuperación de cuenta (`33-security-cross-audit.md` §1.1, MEDIA):** `resetPasswordAllowed: false` — sin "¿olvidaste tu contraseña?" funcional. Depende de DEC-009 (proveedor de correo), todavía `TBD` — ver nota actualizada en `22-decision-log.md` (retiro de autenticación básica de Hotmail en 2026, Resend identificado como alternativa viable).
- **TBD — CI real:** ya se sabe que está pendiente por decisión explícita del usuario (sin repo remoto todavía). Cuando exista un remoto, activar `backend-ci.yml`/`dependabot.yml` (ya escritos) y confirmar que de verdad bloquean un PR con un fallo real, no solo que el YAML es válido.
- **TBD — Cerrar el gap de compartir en iOS (`IOS-004`):** el UI ya existe (`ShareView.swift`/`InvitationsView.swift`) — falta verificarlo con un test real, como ya se hizo en Android/Web.
- **TBD — Autorregistro en iOS:** sin ningún trabajo — `LoginScreen`/`AuthManager` de iOS no tienen botón ni flujo de registro equivalente al de Android/Web.
- ~~Verificación en dispositivo físico de `AND-008`/`AND-009`/`UX-006` (Android)~~ — **cerrado 2026-08-17**, ver `01-technical-backlog.md`. Encontrados y corregidos 2 bugs reales de navegación en el proceso (post-login aterrizaba en Home en vez de Tareas; `startDestination` sin `remember{}` recalculándose en cada recomposición).
- **TBD — Continuar el pulido visual a iOS:** el sistema de diseño (`UX-001`..`UX-006`) nunca llegó a `ios/` (pausado por decisión explícita del usuario desde antes de que existiera `02-ux-ui/`). Cuando iOS se retome, aplicar los mismos tokens/componentes de dashboard que ya tienen Android/Web.
- ~~Condición de carrera real en `UserSyncService.syncFromToken`~~ — **cerrado 2026-08-17** (`BE-036`, ver `01-technical-backlog.md`). Reintento vía proxy auto-inyectado (`@Lazy UserSyncService self`) en una transacción nueva, ya que Postgres aborta la transacción completa tras la violación de constraint — un reintento dentro del mismo método `@Transactional` no habría funcionado. Verificado con un test de concurrencia real (8 hilos simultáneos contra Testcontainers Postgres, 7 de 8 dispararon la violación real internamente) — los 8 resolvieron 200, exactamente una fila creada. `./mvnw clean test`: 112/112.
- **TBD — Reanudar iOS al nivel de Android/Web en general:** más allá del sistema de diseño — `IOS-004`/`IOS-005` (sharing, push) siguen `TODO`, y el retest pendiente en iPad físico (`IOS-002`, quedó inconcluso) sigue sin repetirse.
- ~~Accesibilidad, auditada contra el código real~~ — **cerrado 2026-08-17** (`ACC-001`, ver `01-technical-backlog.md`/`accessibility.md`). Recalculó contraste contra el tema oscuro completo de `UX-006` (6 fallos reales de AA encontrados y corregidos), corrió axe-core + Lighthouse reales en Web (0 violaciones; 1 hallazgo real de Lighthouse — `<main>` faltante — corregido) y una pasada manual real con TalkBack en Android (sin dispositivo del usuario mientras estaba libre; el emulador quedó configurado como `vc_a11y_test` por si se necesita de nuevo). **TBD restante, más acotado que antes:** VoiceOver en iOS (mismo gap general de iOS ya declarado abajo) y un gate de CI real para el spec de axe-core (depende del mismo gap de "CI real" del punto 8 de la sección 1 — sin repo remoto todavía).
- **TBD — Conectar observabilidad a un entorno real:** Crashlytics/GlitchTip hoy son solo desarrollo local; conectarlos a `staging`/producción depende de `INFRA-004` (servidor propio, todavía `TODO`), fuera del control de esta lista por sí sola.
- **TBD — Modo oscuro cálido para la identidad "Agenda" (`UX-008`, DECISION explícita del usuario: pausado, no cancelado):** Web tiene solo modo claro desde `UX-008` — el modo oscuro real y completo que existía (paleta índigo, `UX-006`) se removió en vez de dejarlo mezclado con la paleta nueva (se habría visto como un bug). Diseñar una variante oscura coherente con beige/azul/terracota es el mismo tamaño de esfuerzo que el modo claro (paleta + contraste WCAG verificado, `design-system.md` §9) — no decidido cuándo se retoma.
- **TBD — Sincronizar el theme de Keycloak (`vida-cotidiana-web`) con la paleta final de `UX-008`:** el theme se homologó visualmente contra el diseño de `LoginPage.tsx` en una tarea aparte el mismo día, con sus propios valores hex (ya verificados AA de forma independiente) — no se resincronizó exactamente con los valores recalculados de `index.css` en la pasada de centralización. Ajuste menor, cosmético, no bloqueante.
- **TBD — Concepto real de "evento" (`UX-009`):** Home muestra "Próximos eventos" como mock explícito porque la API no tiene ese concepto — solo "recordatorios/tareas". Si se decide construirlo real, definir primero si es lo mismo que un recordatorio con fecha (client-side, sin backend nuevo) o un tipo de dato distinto (backend nuevo) — decisión de producto, no técnica.
- **TBD — Feed de actividad real (`UX-009`):** "Actividad reciente" en Home es 100% mock — no existe ningún endpoint que exponga actividad al cliente (el audit log de `BE-029` es solo servidor/seguridad). Requeriría una tabla + endpoint nuevos si se decide implementar.
- **TBD — "Invitar a un familiar" (`UX-009`):** el acceso rápido de Home apunta a la pantalla mock de Familia — no existe ningún flujo real de invitar/vincular familiares hoy (compartir es solo por-recordatorio). Si se quiere real, es una decisión de producto/alcance nueva, no solo UI.
- ~~Vistas Semana/Día del Calendario (`UX-010`)~~ — **cerrado 2026-08-18** (`UX-013`, Bloque A). Pill selector Día/Semana/Mes, timeline reutilizando `useCalendarData()` sin cambios. Ver `01-technical-backlog.md`.
- **TBD — Fase 5+ de React Aria + Motion (`UX-011`):** Fases 1-4 cerradas y verificadas (Calendario, ShareDialog, filtro de Inventario, confirmación de Revoke). Un barrido completo de `src/` (vía subagente, antes de decidir Fase 3/4) no encontró más componentes hechos a mano con semántica realmente rota — el resto de la app son listas mock sin interacción real o botones ya correctos. Siguiente trabajo no iniciado todavía requiere la misma validación previa contra un gap real antes de tocar cualquier cosa, por instrucción explícita del usuario de no migrar "porque sí". Candidatos NO evaluados a fondo (mencionados, no confirmados): `Menu` para navegación, `DateField` para el campo de fecha de recordatorios (nota: el `datetime-local` nativo actual ya es razonablemente accesible — reemplazarlo sería re-skinning, no cerraría un gap real, evaluar con cuidado antes de tocarlo).
- **TBD — Test e2e permanente para el flujo completo de Revoke (`UX-011` Fase 4):** verificado real y a fondo manualmente en la sesión (ver `design-system.md` §10), pero requiere un colaborador con share `ACTIVE` real, que solo se logra aceptando una invitación como `userb` — no automatizable sin su contraseña real de Keycloak (desconocida en este entorno) o un mecanismo de datos de prueba con fixtures (no existe todavía). No se intentó adivinar la contraseña para no disparar la protección real de fuerza bruta.
- ~~Backend real para Garantías/Mantenimiento~~ — **cerrado 2026-08-18** (`BE-037`/`WEB-009`/`FR-020`, Bloque B.1). Tablas + migración Flyway, endpoints REST completos (CRUD + completar), estado `COMPLETADO` real y persistido (reemplaza el toggle local-only que se perdía al recargar). Ver `01-technical-backlog.md`/`09-data-model.md`.
- ~~ADR-015 (Personal/Laboral) — implementación de navegación (Bloque C)~~ — **cerrado 2026-08-18** (`UX-012`). Onboarding real, selector superior condicional, navbar de 4 ítems por modo, Ajustes para activar el modo restante, tema Laboral (paleta WCAG-verificada, `design-system.md` §11). El TBD de "¿desactivar un modo desde Ajustes?" sigue abierto, no bloqueante (solo-activar implementado, ver `01-technical-backlog.md` BE-038); "lista exacta de items del navbar" quedó resuelta en Inicio/Calendario/Tareas/Compartidos, sin más.

## Nota de prioridad, 2026-08-18 (decisión explícita del usuario)
Android e iOS quedan pausados como pendientes hasta contar con una versión beta de Web. El trabajo de V2 continúa exclusivamente sobre Web hasta ese punto — no se trata de una re-priorización silenciosa: se deja registrado aquí porque cambia el orden de ejecución respecto a la nota anterior ("Android+Web en paralelo, iOS pausado") sin alterar ninguna decisión arquitectónica (`ADR-001`, `ADR-002`, `ADR-010` siguen vigentes sin cambios).

## Nota de alcance en paralelo, 2026-08-18 (decisión explícita del usuario)
Tres líneas de trabajo avanzan a la vez sobre Web, coordinadas solo donde existe una dependencia real (Frontend → Backend → Integración), sin bloquearse entre sí:
1. **Calendario UI/UX** — vistas Semana/Día (`UX-010`, hoy solo existe vista de Mes). Si depende de datos que el backend todavía no expone, usa mocks temporales identificados explícitamente, sin bloquear el frontend.
2. **Backend de módulos del Calendario** — Garantías/Mantenimiento reales (hoy `MockData`, checkbox local-only que se pierde al recargar; ver §3 arriba) y los campos de `ADR-015` (`USER.personal_enabled/laboral_enabled`, `REMINDER.context`). Reutiliza los endpoints/patrones ya existentes de `reminder` (mismo patrón CRUD + autorización por recurso).
3. **ADR-015 (Personal/Laboral)** — adelantada de V3-diferida a **implementación en paralelo con V2** (ver `22-decision-log.md`). Documentación funcional ya completa (FR-014 a FR-019); implementación empieza ahora. TBD restantes explícitos (no bloqueantes, continuar con lo demás): ¿un modo habilitado se puede desactivar desde Ajustes, o solo activar?; lista exacta de items del navbar por modo más allá de Inicio/Calendario/Tareas/Compartidos.

## Regla de evolución (recordatorio, `02-roadmap.md`)
Una versión no debe requerir rehacer el dominio anterior. Los módulos se agregan detrás de contratos estables — nada de lo propuesto arriba toca el modelo de datos, la autenticación (`ADR-008`) ni el estilo arquitectónico (`ADR-001`, monolito modular).
