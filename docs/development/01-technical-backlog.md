# 01 — Backlog Técnico de Implementación V1

Traducción de la documentación aprobada (`03-prd.md`, `04-use-cases.md`, `13-acceptance.md`, `openapi/openapi.yaml`, `09-data-model.md`, `26-v1-backlog.md`) en tareas técnicas ejecutables. Ninguna tarea introduce una funcionalidad no documentada. Cada tarea referencia su origen (`FR`/`UC`/`AC`/`US`).

Estado: `DONE` (código completo **y** verificado con build/test real), `PARTIAL` (código completo, verificado solo estáticamente — sin build/test real ejecutado, ver `02-validation-report.md`), `BLOCKED` (no se puede completar por una dependencia externa), `TODO` (siguiente).

**Nota de esta actualización (2026-08-15, ver `03-milestone-1-gate.md`, `MILESTONE_1_STATUS: READY`):** `./gradlew clean test` y `./gradlew build` se ejecutaron realmente (JDK 21 + Docker reales) con resultado `BUILD SUCCESSFUL` y 19/19 tests en verde (`02-validation-report.md` §8). Los ítems de backend de Milestone 1 (`BE-001` a `BE-013`, `BE-030` a `BE-032`) pasan de `PARTIAL` a `DONE` en esta actualización — código completo **y** verificado con build/test real, no solo estáticamente.

**Nota adicional (mismo día, tras la migración de build tool a Maven, ADR-013):** con el gate de Milestone 1 confirmado `READY` bajo Maven (`02-validation-report.md` §9, addendum en `03-milestone-1-gate.md`), arrancó Milestone 2 con `BE-014` (`PATCH /reminders/{id}`). `./mvnw clean test`: 26/26 en verde (19 heredados + 7 nuevos de `BE-014`); `./mvnw clean package`: `BUILD SUCCESS`.

**Nota sobre `BE-015` (alcance reducido, redactada el mismo día que `BE-014`, antes de `BE-016..022`):** `openapi.yaml` describe `DELETE /reminders/{id}` como "cascades to INVITATION/REMINDER_SHARE and notifies active collaborators before deletion (DEC-002)". En ese momento `REMINDER_SHARE`/`INVITATION` (`BE-016`..`021`) y el envío de push (`BE-025`) todavía no existían en el código — no había nada que cascadear ni nadie que notificar. `BE-015` implementó el subconjunto del contrato válido con lo que existía entonces (borrado del `REMINDER` propio, autorización por propiedad, `204`), declarado explícitamente, no simulado.

**Actualización (mismo día, tras `BE-016`):** con `V2__sharing.sql` aplicada (`ON DELETE CASCADE` en `invitations.reminder_id`/`reminder_shares.reminder_id`), `ReminderService.delete` ahora sí cascada de verdad a nivel de base de datos — sin ningún cambio de código en `ReminderService`, la cascada era una propiedad de la migración, no de la lógica de aplicación. Sigue faltando únicamente la notificación push a colaboradores activos (segunda mitad de `AC-013`), que sigue dependiendo de `BE-025`/`BE-026` (push, todavía `TODO`) — declarado igual de explícitamente en el código (`ReminderService.delete`) y aquí, no oculto.

**Nota sobre `BE-016` a `BE-022` (sharing completo, 2026-08-15):** implementados y validados en real tras la corrección de infraestructura de la Parte 1 de esta actualización (AWS → servidor propio, `ADR-014`; `DEC-009`/correo reabierta). `./mvnw clean test`: **72/72 en verde** (33 heredados de `BE-001..015` + 39 nuevos: `SharingFlowIntegrationTest` 16, `SharingServiceTest` 19, `ReminderServiceTest` +4 casos de autorización de colaborador para `BE-022` — desglose completo en `docs/development/02-validation-report.md` §11). `./mvnw clean package`: `BUILD SUCCESS`. Ningún endpoint ni campo se desvió del contrato ya definido en `openapi.yaml`/`09-data-model.md`; el único ajuste de alcance explícito es `DEVOPS-001` (rate limiting), deliberadamente excluido de este incremento.

**Nota sobre `BE-023` a `BE-028`, `BE-033`, `BE-034`, `DEVOPS-001`/`002` (push, eliminación de cuenta, rate limiting, SAST — 2026-08-15):** `./mvnw clean test`: **101/101 en verde** (72 heredados + 29 nuevos: `SharingFlowIntegrationTest` +2, `SharingServiceTest` +1, `InvitationMaintenanceServiceTest` 2, `DeviceControllerIntegrationTest` 5, `DeviceRegistrationServiceTest` 7, `FcmPushNotificationSenderTest` 3, `ReminderServiceTest` +1, `AccountDeletionIntegrationTest` 2, `AccountDeletionServiceTest` 3, `UserSyncServiceTest` 3 — desglose completo en `02-validation-report.md` §12). `./mvnw clean package`: `BUILD SUCCESS`. Se encontraron y corrigieron dos gaps reales durante esta validación (`BE-033`, `BE-034`), mismo patrón que `BE-030..032`.

**Nota sobre `BE-029`, `TEST-API-001`, `INFRA-002`, `INFRA-003`, `AND-001`, `IOS-001`, `WEB-001` (auditoría, contract tests, Keycloak/CI, bootstrap de clientes — 2026-08-15):** `./mvnw clean test`: **108/108 en verde** (101 heredados + 7 nuevos: `AuditEventIntegrationTest` 6, `InvitationMaintenanceServiceTest` +1 — desglose completo en `02-validation-report.md` §13). `./mvnw clean package`: `BUILD SUCCESS`. Se encontró y corrigió un hallazgo real durante el contract testing de `TEST-API-001` (`BE-035`, ver abajo). Para el bloque de bootstrap de clientes: Android/iOS quedan **BLOCKED_BY_ENVIRONMENT** en el paso de build real (sin Android SDK / sin Xcode.app completo en este entorno, solo Command Line Tools) — declarado explícitamente, no simulado; Web sí se instaló y compiló realmente (`npm install`/`npm run build`, Node disponible).

**BE-035 — Hallazgo propio de esta validación (`TEST-API-001`):** dos desviaciones reales entre `openapi.yaml` y la implementación, encontradas al validar respuestas reales contra el spec real (no una copia) con `swagger-request-validator-mockmvc`. (1) `ReminderResponse` serializaba `description`/`dueAt` como JSON `null` cuando el reminder no los tenía, pero `Reminder` en `openapi.yaml` los declara `type: string`/`type: string, format: date-time` sin `nullable: true` (a diferencia de `ReminderShare.revokedAt`, que sí lo declara) — corregido en el código (`@JsonInclude(JsonInclude.Include.NON_NULL)` en `ReminderResponse`), no relajando el contrato. (2) los tres endpoints paginados (`GET /reminders`, `GET /reminders/{id}/shares`, `GET /me/invitations`) usan `allOf: [PageMeta, {items/shares+invitations}]`; el validador rechazaba cada instancia real porque interpreta cada rama de `allOf` como un schema cerrado (sin fusionar propiedades), rechazando los campos de la rama hermana como "additionalProperties no permitido" — la implementación ya era exactamente la unión que el contrato pretende describir, así que aquí el defecto real estaba en la composición del schema, no en el código; corregido añadiendo `additionalProperties: true` explícito a `PageMeta` y a cada rama inline de `allOf` en `openapi.yaml` (no afloja ningún tipo/campo/`required` ya validado, solo corrige la interacción `allOf`+`additionalProperties` de esta librería).

**Nota sobre `AND-002`/`AND-003`, `WEB-002`/`WEB-003`, clientes OIDC de `INFRA-002` (login + reminders CRUD real, 2026-08-15):** Android SDK instalado por terminal (`cmdline-tools` + `sdkmanager`, sin Android Studio); Xcode/iOS quedó **bloqueado** — `/opt/homebrew` pertenece a otro usuario del Mac y `sudo` requiere una contraseña interactiva que este entorno no puede proveer, así que `brew install xcodes` no pudo ejecutarse (ver `03-milestone-1-gate.md`/reporte de cierre de esta sesión para el comando exacto pendiente). Se añadieron 3 clientes OIDC públicos con PKCE S256 a `infra/keycloak/realm-vida-cotidiana.json` (`android-app`, `ios-app`, `web-spa`) y se verificó el flujo Authorization Code + PKCE completo contra un Keycloak real (script headless, sin navegador) antes de escribir ningún código de cliente. Web: login PKCE real + CRUD de reminders verificado con un navegador headless real (Playwright, Chromium) haciendo clic a través de la UI real — encontró y corrigió un bug real (React StrictMode reintentaba el intercambio del authorization code, que es de un solo uso). Android: login (AppAuth-Android) + reminders (Retrofit/Hilt) compilan y empaquetan de verdad (`./gradlew clean assembleDebug`, `BUILD SUCCESSFUL`, APK real de ~36 MB), con el redirect de AppAuth verificado en el manifest ya mergeado del APK (`aapt dump xmltree`) — pero sin dispositivo/emulador conectado en este entorno, nadie tocó la UI real (`adb devices` no lista nada), declarado como `BLOCKED_BY_ENVIRONMENT`, no simulado. iOS (`IOS-002`/`IOS-003`) sigue `TODO`, bloqueado por lo mismo que `IOS-001`. `./mvnw clean test`: 108/108 siguen en verde tras añadir CORS al backend (necesario para que el navegador permitiera las llamadas de `web-spa`, `SecurityConfig` con `vida-cotidiana.cors.allowed-origins`, default `http://localhost:5173`).

**Nota explícita sobre `AND-006`/`WEB-006`/`INFRA-007` (reporte de errores, 2026-08-16):** Firebase Crashlytics y GlitchTip autoalojado son, en este momento, **error-tracking de desarrollo local únicamente** — GlitchTip corre en `docker-compose.yml` (infraestructura local, igual que Keycloak/Postgres, con el mismo comentario de "no producción" en el archivo) y Crashlytics reporta contra el mismo proyecto Firebase de desarrollo usado para `AND-005`. Ninguno de los dos está conectado todavía a un entorno real/`staging`/producción — eso depende de `INFRA-004` (servidor propio, todavía `TODO`). No confundir "verificado con evidencia real" (el pipeline funciona de extremo a extremo, tal como se probó) con "desplegado en un entorno real" (no lo está). Alcance de esta sesión fue deliberadamente acotado a estas dos integraciones — CI, auditoría de seguridad, y cualquier otra herramienta de observabilidad quedan fuera, en ese orden según lo acordado.

**TBD explícito (no resuelto en código, por instrucción directa de la tarea):** qué ocurre con `REMINDER`/`REMINDER_SHARE`/`INVITATION` propiedad de o compartidos por una cuenta purgada (`BE-027`) — ninguna decisión aprobada (`DEC-002`, `DEC-015`, `28-v1-decision-pack.md`) especifica si esos recordatorios deben transferirse, eliminarse o quedar huérfanos con `owner_user_id` apuntando a una cuenta `DELETED`. Inventar ese comportamiento violaría la regla fundamental de no inventar requerimientos de negocio (CLAUDE.md). `User.purge()` anonimiza únicamente la fila `USER`; no toca ninguna otra tabla. Requiere una decisión explícita del Product Owner antes de implementarse.

**Nota sobre `AND-007`/`WEB-007` y la auditoría cruzada de seguridad (2026-08-16):** los dos gaps de V1 identificados en `05-v2-plan.md` (notificaciones locales, secretos nunca escaneados de verdad) se cerraron en la misma sesión — ver las filas `AND-007`/`WEB-007` abajo y `Documentacion/33-security-cross-audit.md` §5. La auditoría de seguridad, ya acordada como siguiente paso en la nota de arriba (`AND-006`/`WEB-006`/`INFRA-007`), se corrió de verdad contra `21-security.md`/`24-security-checklist.md`/`11-auth-security.md` y el código real — encontró dos `DOCUMENTATION_CONFLICT` reales (protección contra fuerza bruta y recuperación de cuenta declaradas como resueltas por Keycloak en `11-auth-security.md`/ADR-008, pero `bruteForceProtected`/`resetPasswordAllowed` están en `false` en el realm real) y un gap no documentado antes (autorregistro no funciona en ningún cliente — `registrationAllowed: false`, sin UI de registro en Android/Web). Ninguno de los tres se corrigió en esta sesión — quedan como decisiones pendientes del Product Owner en `05-v2-plan.md` §3, no resueltas en silencio. Detalle completo, con severidad por hallazgo, en `Documentacion/33-security-cross-audit.md`.

## Backend (Java 21 / Spring Boot 3.3.4 / Modular Monolith)

| ID | Tarea | Origen | Estado | Clases / Tests |
|---|---|---|---|---|
| BE-001 | Bootstrap del proyecto backend (**Maven**, `pom.xml` + wrapper `./mvnw`; originalmente Gradle, migrado 2026-08-15 por decisión del Product Owner, ver ADR-013), estructura de paquetes por módulo (`shared/identity/user/reminder/sharing/notification/audit`) | `07-backend-architecture.md` | DONE | `pom.xml`, `mvnw`/`mvnw.cmd`/`.mvn/wrapper/` |
| BE-002 | `docker-compose.yml` local (PostgreSQL 16 + Keycloak 25 dev) | `18-dev-environment.md` | DONE | `docker-compose.yml` (Keycloak levantado vía `docker compose`; Postgres del compose validado en un puerto alternativo por conflicto de puerto local, ver `02-validation-report.md` §8.7) |
| BE-003 | Migración Flyway `V1__init_schema.sql` (`users`, `reminders`) | `09-data-model.md` | DONE | `V1__init_schema.sql` (aplicada realmente por Flyway contra un PostgreSQL 16 real; Hibernate validó el esquema sin discrepancias, `02-validation-report.md` §8.7) |
| BE-004 | Resource server OAuth2/OIDC (Spring Security, sin login propio) | `11-auth-security.md`, ADR-008 | DONE | `SecurityConfig`, `RestAuthenticationEntryPoint`, `RestAccessDeniedHandler` (nuevas en este ciclo) |
| BE-005 | Sincronización de `USER` desde JWT (upsert por request autenticado) | UC-01/UC-02, `09-data-model.md` | DONE | `UserSyncFilter`, `UserSyncService`; `UserControllerIntegrationTest` (nueva en este ciclo) |
| BE-006 | `GET /me` | `openapi.yaml` | DONE | `UserController`; `UserControllerIntegrationTest` |
| BE-007 | `POST /reminders` (crear) | UC-03, AC-003, FR-004 | DONE | `ReminderController.create`; `ReminderControllerIntegrationTest.createListAndCompleteReminder_happyPath`, `.createReminder_blankTitleIsRejected` |
| BE-008 | `GET /reminders` (listar propios, paginado) | AC-004, FR-003/FR-004 | DONE | `ReminderController.list`; `ReminderControllerIntegrationTest.createListAndCompleteReminder_happyPath` |
| BE-009 | `GET /reminders/{id}` | AC-004 | DONE | `ReminderController.get`; `.getReminder_ownedByAnotherUserReturnsNotFound_neverForbidden`, `.getReminder_missingIdReturnsNotFound` |
| BE-010 | `POST /reminders/{id}/complete` (bloqueo optimista opcional) | UC-04, AC-005 | DONE | `ReminderController.complete`; `.completeReminder_mismatchedVersionReturns409`, `.completeReminder_omittedVersionSkipsConcurrencyCheck`; `ReminderServiceTest` (7 casos) |
| BE-011 | Manejo uniforme de errores (`Error` schema, `traceId`) | AC-006, NFR-006 | DONE | `GlobalExceptionHandler` + (nuevo) `RestAuthenticationEntryPoint`/`RestAccessDeniedHandler` para errores a nivel de filtro de seguridad, que antes NO pasaban por `GlobalExceptionHandler` (ver `03-milestone-1-gate.md` §Correcciones, hallazgo 1) |
| BE-012 | Tests unitarios `ReminderService` | `20-testing-qa.md` | DONE | `ReminderServiceTest` (8 tests: crear, acceso propietario/ajeno, completar, versión correcta/incorrecta/omitida) |
| BE-013 | Tests de integración (Testcontainers + MockMvc): crear/listar/completar/acceso ajeno/conflicto de versión | `20-testing-qa.md` | DONE | `ReminderControllerIntegrationTest` (8 tests), `UserControllerIntegrationTest` (3 tests, nueva) |
| BE-030 | Fix: filtros `TraceIdFilter`/`UserSyncFilter` se ejecutaban dos veces por request (auto-registro genérico de Spring Boot + registro explícito en `SecurityConfig`) | Hallazgo propio de esta validación | DONE | `SecurityConfig` (`FilterRegistrationBean` deshabilitando el auto-registro) |
| BE-031 | Fix: 401/403 generados dentro de la cadena de Spring Security no llevaban el envoltorio `Error` (`code`/`message`/`traceId`) | Hallazgo propio de esta validación, AC-006 | DONE | `RestAuthenticationEntryPoint`, `RestAccessDeniedHandler`; `ReminderControllerIntegrationTest.reminderEndpoints_requireAuthentication`, `UserControllerIntegrationTest.getCurrentUser_requiresAuthentication` |
| BE-032 | Fix: excepción no controlada dentro de `UserSyncFilter` (p. ej. token sin claim `email`) escapaba de `GlobalExceptionHandler` | Hallazgo propio de esta validación, AC-006 | DONE | `UserSyncFilter` (try/catch + respuesta `Error` uniforme) |
| BE-014 | `PATCH /reminders/{id}` (editar, bloqueo optimista obligatorio) | AC-004b, US-005 | DONE (2026-08-15, Milestone 2) | `ReminderController.update`, `ReminderService.edit`, `Reminder.applyEdit`, `UpdateReminderRequest`; `ReminderServiceTest` (+3 casos), `ReminderControllerIntegrationTest` (+4 casos). `./mvnw clean test`: 26/26 en verde; `./mvnw clean package`: `BUILD SUCCESS`. |
| BE-015 | `DELETE /reminders/{id}` | UC-05, AC-013, DEC-002 | DONE — **alcance reducido** (2026-08-15, ver nota abajo) | `ReminderController.delete`, `ReminderService.delete`; `ReminderServiceTest` (+3 casos), `ReminderControllerIntegrationTest` (+4 casos). `./mvnw clean test`: 33/33 en verde; `./mvnw clean package`: `BUILD SUCCESS`. |
| BE-016 | Migración Flyway `V2__sharing.sql` (`invitations`, `reminder_shares`) | `09-data-model.md` | DONE (2026-08-15) | `V2__sharing.sql` — aplicada realmente por Flyway (`Migrating schema "public" to version "2 - sharing"`, confirmado en cada test de integración). Incluye `ON DELETE CASCADE`, `UNIQUE(reminder_id, collaborator_user_id)`, índice único parcial `WHERE status = 'PENDING'`, e índices de `invited_email`/`(status, expires_at)`/`collaborator_user_id` — todos los obligatorios de `09-data-model.md`, sin añadir ninguno extra. |
| BE-017 | `POST /reminders/{id}/shares` (crear invitación, sin enumeración) | UC-07, AC-007, SEC-001 | DONE (2026-08-15) | `ReminderShareController.create`, `SharingService.createInvitation`; adapter de correo `sharing.infrastructure.NoOpEmailSender` (no-op/log-only, DEC-009 reabierta — ver Parte 1 de esta actualización, `22-decision-log.md` ADR-014). Rate limiting explícitamente **no** incluido (`DEVOPS-001` sigue `TODO`, incremento aparte). `SharingServiceTest` (8 casos: email con/sin cuenta, username existente/inexistente, ambos/ninguno, duplicada→409, no-owner→404), `SharingFlowIntegrationTest` (5 casos). |
| BE-018 | `GET /reminders/{id}/shares`, `GET /me/invitations` (paginado) | `openapi.yaml` | DONE (2026-08-15) | `ReminderShareController.list`, `InvitationController.myInvitations`, `SharingService.listSharesAndInvitations`/`listMyPendingInvitations`; `SharingFlowIntegrationTest` (listado owner/no-owner, `/me/invitations`). |
| BE-019 | `POST /invitations/{id}/accept` / `/reject` (transición atómica condicional) | UC-08/UC-09, AC-008/AC-009, HIG-003 | DONE (2026-08-15) | `InvitationController.accept`/`.reject`, `SharingService.acceptInvitation`/`.rejectInvitation`, `InvitationRepository.resolveIfPending` (`@Modifying` `UPDATE ... WHERE status = 'PENDING'`, nunca "leer, comprobar en Java, guardar"). `SharingServiceTest` (accept/reject happy path + ya resuelta→410 + no-destinatario→404), `SharingFlowIntegrationTest` (incluye dos resoluciones secuenciales sobre la misma invitación → la segunda da 410, AC-008). |
| BE-020 | `DELETE /invitations/{invitationId}` (cancelar, UC-14) | AC-017 | DONE (2026-08-15) | `InvitationController.cancel`, `SharingService.cancelInvitation` (mismo patrón de transición atómica). `SharingServiceTest`/`SharingFlowIntegrationTest` (owner→204, no-inviter→404, ya resuelta→410). |
| BE-021 | `DELETE /reminders/{id}/shares/{shareId}` (revocar, efecto inmediato) | UC-10, AC-010 | DONE (2026-08-15) | `ReminderShareController.revoke`, `SharingService.revokeShare` (idempotente sobre un share ya `REVOKED` — `openapi.yaml` no documenta un estado de conflicto para ese caso). `SharingFlowIntegrationTest.revokeShare_effectiveImmediately`: confirma que el colaborador revocado recibe `404` en el siguiente request, sin ventana de gracia. |
| BE-022 | Autorización `OWNER`/`COLLABORATOR` transversal sobre recordatorios compartidos | `11-auth-security.md` | DONE (2026-08-15) | `ReminderService` extendido: `requireOwnerOrActiveCollaborator` (usado por `getAccessible`/`toggleCompletion`) vs. `requireOwner`/`getOwnedOrThrow` (usado por `edit`/`delete`, y reutilizado por `SharingService` para invitar/listar/revocar — owner-only). `ReminderRepository.findAccessibleTo` amplía `GET /reminders` a propios + compartidos `ACTIVE` (FR-003/FR-004). `ReminderServiceTest` (+4: colaborador puede leer/completar, no puede editar/eliminar), `SharingFlowIntegrationTest.collaborator_canReadAndCompleteButNeverEditDeleteOrInvite`. |
| BE-023 | Migración Flyway `V3__push.sql` (`device_push_tokens`) | `09-data-model.md` | DONE (2026-08-15) | `V3__push.sql` — aplicada realmente por Flyway (`Migrating schema "public" to version "3 - push"`, confirmado en cada test de integración). `UNIQUE(token)`, `CHECK(platform IN (...))`, índice sobre `user_id` — exactamente lo pedido, nada más. |
| BE-024 | `GET/POST /me/devices`, `DELETE /me/devices/{deviceId}` | UC-12, AC-014, FR-012 | DONE (2026-08-15) | `DeviceController`, `DeviceRegistrationService` (upsert por `token`, DEC-005). **AC-014 aplicado literalmente:** eliminar un dispositivo ajeno responde `403` (vía `AccessDeniedException`, reutilizando el handler ya existente), no el `404` uniforme de Reminder/sharing — distinción real documentada en el código, no "corregida" para uniformar. `DeviceRegistrationServiceTest` (7 casos), `DeviceControllerIntegrationTest` (5 casos, incluye upsert reasignando token entre cuentas y el 403 real). |
| BE-025 | Puerto `PushNotificationSender` + adapter FCM | ADR-007, DEC-010 | DONE (2026-08-15) | `notification.application.PushNotificationSender` (puerto), `notification.infrastructure.NoOpPushNotificationSender` (bean por defecto), `notification.infrastructure.FcmPushNotificationSender` (adapter real, dependencia `com.google.firebase:firebase-admin:9.10.0`, justificada por DEC-010) + `PushNotificationConfig` (`@ConditionalOnProperty(firebase.credentials-path)` activa FCM; `@ConditionalOnMissingBean` deja el no-op como fallback — no configurado en este entorno ni en ningún perfil de test, así que el no-op es el bean real usado en toda la suite). AC-012 (best-effort) es un contrato del propio método: las implementaciones capturan y loguean, nunca propagan. `FcmPushNotificationSenderTest` (3 casos): **construcción del payload verificada contra un `FirebaseMessaging` mockeado; el envío real a Firebase NO es verificable en este entorno** (sin proyecto Firebase real disponible) — declarado explícitamente, no simulado como "push real probado". |
| BE-026 | Eventos de push (invitación recibida/aceptada/rechazada/cancelada, revocación, eliminación) | UC-11, AC-012, FR-011 | DONE (2026-08-15) | `SharingService` (invitación creada→invitado si tiene cuenta; aceptada/rechazada→inviter; cancelada→invitado si tiene cuenta) y `ReminderService.delete` (notifica a cada colaborador `ACTIVE` antes del borrado, cerrando la segunda mitad de `AC-013` que `BE-015` había dejado explícitamente pendiente). Ningún evento fuera de la lista exacta de AC-012 (p. ej. "recordatorio editado" no está en esa lista, no se añadió). Cobertura en `SharingServiceTest`/`ReminderServiceTest`/`SharingFlowIntegrationTest` (verificación de `sendBestEffort` en cada punto). |
| BE-027 | `DELETE /me` + `PENDING_DELETION` + job de purga a 30 días | UC-13, AC-015, DEC-015 | DONE (2026-08-15) | `UserController.deleteCurrentUser` (siempre "yo", sin parámetro), `AccountDeletionService.requestDeletion`/`.purgeAccountsPastGracePeriod` (`@Scheduled` cada hora, `@EnableScheduling` en `BackendApplication`), `User.requestDeletion`/`.purge` (anonimiza `email`/`username` únicamente — `REMINDER`/`REMINDER_SHARE`/`INVITATION` del usuario purgado **no se tocan**, ninguna decisión aprobada especifica su destino, ver nota TBD abajo). Sin endpoint de cancelación/reversión (no documentado en `openapi.yaml`, `09-data-model.md` lo marca TBD de UX) y sin bloqueo de login durante `PENDING_DELETION` (UC-13 paso 3 TBD; el backend no gestiona sesiones, DEC-004/ADR-008). **Hallazgo real corregido durante esta validación, ver `BE-034` abajo.** `AccountDeletionServiceTest` (3 casos), `AccountDeletionIntegrationTest` (2 casos: purga real forzando `purge_at` al pasado + verificación de que una cuenta aún dentro del periodo de gracia no se toca). |
| BE-028 | Job de purga de invitaciones sin cuenta asociada (retención corta) | AC-016, DEC-015 (A') | DONE (2026-08-15) | `sharing.application.InvitationMaintenanceService.purgeOrphanedInvitations` (`@Scheduled` cada hora, offset 30 min del job de expiración), `InvitationRepository.purgeOrphaned` (`DELETE` de la fila completa — la opción más simple de las dos ya documentadas como equivalentes en `09-data-model.md`, no una decisión nueva; retención 90 días, ASSUMPTION ya documentada). `InvitationMaintenanceServiceTest` (2 casos). |
| BE-029 | Auditoría de eventos de seguridad (creación/resolución de invitación, revocación) | `11-auth-security.md` §Auditoría | DONE (2026-08-15) | Entidad `AUDIT_EVENT` nueva, añadida explícitamente a `09-data-model.md` (no existía en ninguna decisión previa) — `id, event_type, actor_user_id (FK nullable), target_type, target_id, occurred_at`, sin columna de detalle libre/JSON (deliberado, evita fuga futura de secretos), índices en `(target_type, target_id)` y `occurred_at`. `V4__audit.sql`, `audit.domain.AuditEvent`/`AuditEventRepository`, `audit.application.AuditEventService.record` (siempre en la misma transacción que la operación de negocio — nunca best-effort, a diferencia de push). Los 6 eventos de `11-auth-security.md` §Auditoría cableados en `SharingService` (creación/aceptación/rechazo/cancelación/revocación) y `InvitationMaintenanceService.expireOverduePendingInvitations` (expiración, `actorUserId = null` — job del sistema, no un usuario). Sin endpoint de lectura (no está en `openapi.yaml`, no se inventó uno). `SharingServiceTest`/`InvitationMaintenanceServiceTest` (verifican la llamada a `record`), `AuditEventIntegrationTest` (6 casos, extremo a extremo contra PostgreSQL real vía Testcontainers — confirma que cada una de las 6 transiciones inserta realmente la fila esperada, no solo que se llamó al servicio). |
| BE-033 | Hallazgo propio de esta validación: `INVITATION(status, expires_at)` existía "para el job de expiración" (`09-data-model.md` línea 77) pero ningún job transicionaba `PENDING → EXPIRED`; `resolveIfPending` tampoco comprobaba `expiresAt` | Hallazgo propio de esta validación, respalda AC-016 | DONE (2026-08-15) | `InvitationRepository.expireOverduePending` (sweep atómico `UPDATE ... WHERE status='PENDING' AND expires_at<=now()`) + `resolveIfPending` extendido con `AND expiresAt > now()` en la misma actualización condicional (no una comprobación Java separada — mismo principio de atomicidad que ya usaba `BE-019`). `InvitationMaintenanceServiceTest`, `SharingFlowIntegrationTest.expiredInvitation_cannotBeAcceptedBeforeSweep_thenSweepMarksItExpired` (fuerza `expires_at` al pasado vía JDBC directo, confirma `410` antes del sweep y `EXPIRED` después). |
| BE-034 | Hallazgo propio de esta validación: `UserSyncFilter`/`UserSyncService.syncFromToken` re-sincronizaba `email`/`username` desde el JWT en **cualquier** request autenticado, incluida una cuenta ya `DELETED` (purgada) — cualquier request posterior del mismo usuario (el backend no gestiona ni revoca sesiones de Keycloak) deshacía silenciosamente la anonimización, violando AC-015 | Hallazgo propio de esta validación, AC-015 | DONE (2026-08-15) | `UserSyncService.syncFromToken`: guarda `deletionStatus == "DELETED"` antes de llamar a `refreshFromIdentityProvider`. Encontrado exactamente por el mismo mecanismo que `BE-030`..`032`: escribir el test de integración real de `BE-027` (`AccountDeletionIntegrationTest`) hizo evidente el fallo — un `GET /me` posterior a la purga devolvía el email/username originales, no los anonimizados. `UserSyncServiceTest` (3 casos, incluye el caso exacto: mismas claims del JWT que antes de purgar, la cuenta debe permanecer anonimizada). |
| TEST-API-001 | Contract tests: validar respuestas reales contra `openapi.yaml` (schema `Error`/`PageMeta`) | `20-testing-qa.md` | DONE (2026-08-15) | `com.atlassian.oai:swagger-request-validator-mockmvc:3.0.0` (scope test), apuntando a la ruta real del repo `../Documentacion/openapi/openapi.yaml` (no una copia) vía `OpenApiContractSupport`. Validación de request/response cableada sobre al menos un caso representativo por recurso en los tests de integración ya existentes (no se duplicó la suite): `GET /me`; `POST`/`GET /reminders`; `GET /reminders/{id}` (200 y 404); `PATCH /reminders/{id}` (200 y 409); `POST /reminders/{id}/shares` (201 y 409); `POST /invitations/{id}/accept` (200 y 410); `GET`/`POST`/`DELETE /me/devices` (incluye el 403 de `BE-024`); `DELETE /me` (202). Encontró y corrigió dos desviaciones reales, ver `BE-035` arriba. |
| DEVOPS-001 | Rate limiting (`429`) sobre creación de invitaciones | SEC-001 | DONE (2026-08-15) | `sharing.application.InvitationRateLimiter` — ventana deslizante en memoria (`ConcurrentHashMap<UUID, Deque<Instant>>`), sin dependencia nueva (nada de Bucket4j/Redis, no justificado para una sola instancia en V1). Límite: **10 invitaciones/usuario/hora** (parámetro técnico documentado en el código, no una decisión de negocio). Excedido → `429` con el envoltorio `Error` de siempre (`RateLimitExceededException`, nuevo handler en `GlobalExceptionHandler`). Cobertura: `SharingServiceTest.createInvitation_exceedingRateLimit_returns429` (unitario) + `SharingFlowIntegrationTest.createInvitation_exceedingRateLimit_returns429` (HTTP real). |
| DEVOPS-002 | SAST/lint básico local (sin CI todavía) | `14-definition-of-done.md` | DONE (2026-08-15) | `spotbugs-maven-plugin:4.10.3.0` añadido al `pom.xml`, **no** colgado de ninguna fase del build por defecto (se ejecuta explícitamente con `./mvnw spotbugs:check`, consistente con "sin CI todavía"). Primera ejecución real: **14 hallazgos**, todos `EI_EXPOSE_REP`/`EI_EXPOSE_REP2` ("may expose internal representation"). De esos: **6 reales, corregidos** (`shared.api.PageResponse` y `sharing.api.dto.SharesAndInvitationsResponse` exponían sus `List` internos sin copia defensiva — se añadió `List.copyOf(...)` en el constructor compacto de ambos records). **8 declarados explícitamente como falsos positivos, sin suprimirlos globalmente:** el patrón estándar de inyección de dependencias de Spring (constructor guardando una referencia a un bean singleton — `ObjectMapper`, filtros, servicios) dispara la misma regla, pero copiar defensivamente un bean gestionado por el contenedor no tiene sentido (rompería el grafo de DI) y no representa un riesgo real de mutación externa. Segunda ejecución tras las dos correcciones: **8 hallazgos** (exactamente los 8 falsos positivos declarados). |

## Android (Kotlin / Jetpack Compose)

| ID | Tarea | Origen | Estado |
|---|---|---|---|
| AND-001 | Bootstrap proyecto (Gradle, Compose, Hilt, Coroutines/Flow) | `08-android-architecture.md` | DONE (scaffold) / **BLOCKED_BY_ENVIRONMENT** (build real) (2026-08-15) — `android/` (Gradle Kotlin DSL, wrapper Gradle 8.9 real, `minSdk=30` DEC-011), estructura `app/core/{network,security,ui}`, `app/feature/{auth,home,reminders,sharing,settings}`, `app/navigation` (paquetes vacíos, `.gitkeep`), una pantalla Compose mínima. `./gradlew assembleDebug` ejecutado de verdad: la configuración de Gradle resuelve sin error (plugins, Compose Compiler, dependencias); falla solo en `:app:compileDebugJavaWithJavac` con "SDK location not found" — no hay Android SDK instalado en este entorno (sin `ANDROID_HOME`, sin `sdkmanager`/`adb`). Declarado explícitamente, ver `android/README.md`. |
| AND-002 | Autenticación Keycloak (Authorization Code + PKCE, AppAuth) | UC-01/UC-02 | **DONE, verificado en dispositivo físico real** (actualizado 2026-08-16) — `net.openid:appauth`, cliente `android-app` (público, PKCE S256 generado por AppAuth), Custom Tabs (nunca WebView). Tokens en `EncryptedSharedPreferences` (`core/security/TokenStore.kt`, AES256-GCM, Keystore). Verificado extremo a extremo con un test instrumentado real (`LoginAndRemindersFlowTest`, UI Automator + Compose Testing) corrido de verdad sobre un Samsung Galaxy físico (Android 16) conectado por USB: login real contra Keycloak, redirect real de vuelta a la app, sesión persistida. **Tres bugs reales encontrados y corregidos durante esta verificación:** (1) `RedirectUriReceiverActivity` de AppAuth exige un tema `Theme.AppCompat` — el tema plano de la app lo crasheaba al volver de Keycloak (fix: `res/values/themes.xml`). (2) `DefaultConnectionBuilder` de AppAuth rechaza cualquier conexión no-HTTPS de forma incondicional, bloqueando el intercambio de tokens contra el Keycloak de desarrollo (HTTP plano) — fix: `AppAuthConfigProvider`/`DebugConnectionBuilder`, variantes por source set (`src/debug` permite HTTP local; `src/release` mantiene el default HTTPS-only de AppAuth, sin excepción). (3) `MainActivity` sin `launchMode` se recreaba al volver del Custom Tab, dejando el `ComposeTestRule` (y potencialmente cualquier estado de UI real) enganchado a una instancia obsoleta — fix: `android:launchMode="singleTask"` en el manifest. |
| AND-003 | Reminders: crear/listar/completar (consume BE-007..010) | UC-03/UC-04 | **DONE, verificado en dispositivo físico real** (actualizado 2026-08-16) — Retrofit + `retrofit2-kotlinx-serialization-converter` + OkHttp (interceptor añade `Authorization: Bearer` desde `TokenStore`), `RemindersViewModel`/`RemindersScreen` (Compose) contra `GET/POST /reminders`, `POST /reminders/{id}/complete` reales. Verificado en el mismo test instrumentado real: crea un recordatorio con sufijo aleatorio, confirma que aparece en la lista, lo completa, confirma el cambio de estado — contra el backend real por Wi-Fi (IP LAN del Mac). **Confirmado además por fuera del test**, consultando la base de datos real (`SELECT ... FROM reminders WHERE title LIKE 'Instrumented test reminder%'`): filas reales con `status = COMPLETED`. Un cuarto hallazgo real durante esta verificación: la lista de recordatorios conserva todo lo creado contra el backend real (sin reset entre corridas), así que un botón "Complete" sin distinguir por fila era ambiguo entre corridas — fix: `testTag` por título en cada fila (`RemindersScreen.kt`). `./gradlew clean assembleDebug`: `BUILD SUCCESSFUL`. Refresh-on-401 sigue sin implementar (ASSUMPTION, no decisión — fuera de alcance de este bootstrap). |
| AND-004 | Sharing/Invitations UI | UC-07..UC-10, UC-14 | **DONE, verificado en dispositivo físico real** (2026-08-16) — `core/network/SharingApi.kt` (7 endpoints del contrato, sin inventar ninguno), `feature/sharing/ShareDialog.kt` (panel inline por recordatorio, owner-only, vía `NetworkEntryPoint`/`EntryPointAccessors` en vez de `@AssistedInject` — riesgo de versión evitado en `hilt-navigation-compose:1.2.0`), `feature/sharing/{InvitationsViewModel,InvitationsScreen}.kt` (`GET /me/invitations`, aceptar/rechazar). Verificado extremo a extremo con `SharingFlowTest` (instrumentado real, mismo dispositivo físico que `AND-002/003`): owner invita a `userb` por username, la invitación aparece en el panel — confirmado además por fuera del test consultando Postgres. **Cuatro hallazgos reales durante esta verificación:** (1) `ShareDialog` llamaba `LocalContext.current` (función `@Composable`) dentro del lambda de `remember { }`, que no es un contexto `@Composable` — fix: capturar `context` fuera de `remember`. (2) `build.gradle.kts` tenía `OIDC_ISSUER`/`API_BASE_URL` revertidos al alias de emulador (`10.0.2.2`), inalcanzable desde el dispositivo físico por Wi-Fi — fix: IP LAN del Mac (`192.168.0.18`), igual que `ios/App/Core/Network/AppConfig.swift`. (3) Al corregir el host, `network_security_config_debug.xml` solo permitía cleartext HTTP a `10.0.2.2`/`localhost`, no a la IP LAN — bloqueaba el intercambio de tokens de AppAuth con "Network error" sin que la petición llegara siquiera a Keycloak (confirmado por ausencia total en `docker logs vc-dev-keycloak`) — fix: se añadió el dominio a la whitelist. (4) `RemindersViewModel.refresh()` lanzaba una corrutina independiente en cada llamada sin cancelar la anterior — el `refresh()` inicial de `init{}`, lento en un arranque en frío que compite con el login, podía resolver **después** del `refresh()` posterior a `createReminder()` y sobrescribir silenciosamente la lista con datos obsoletos, ocultando el recordatorio recién creado — fix: `Job` cancelado antes de relanzar. Adicionalmente, `ReminderRepository.findAccessibleTo` (backend) no tenía `ORDER BY`, así que un recordatorio nuevo podía aparecer en cualquier posición de una lista ya larga y quedar fuera del viewport virtualizado de `LazyColumn` — fix: `ORDER BY r.createdAt DESC` (afecta a las tres plataformas por igual, backend compartido). También se corrigió un bug de layout real: el `Row` de cada fila no reservaba espacio para los botones "Complete"/"Share" — un título largo los comprimía a **ancho cero** en el árbol de semántica (presentes pero no clicables) — fix: `Modifier.fillMaxWidth(0.6f)` + elipsis en el título. `./gradlew assembleDebug`: `BUILD SUCCESSFUL`. `./mvnw test` (backend, tras el fix de `ORDER BY`): 108/108 en verde. |
| AND-005 | Registro de push (FCM) | UC-12 | **DONE — registro real verificado; entrega de punta a punta sigue bloqueada** (2026-08-16) — a mitad de esta sesión el usuario terminó de descargar la configuración real de Firebase (`google-services.json`, proyecto `vida-cotidiana-6da30`) mientras se trabajaba en `AND-004`; se integró de verdad: plugin `com.google.gms.google-services` + `firebase-messaging-ktx` (BoM `33.5.1`), `core/network/DeviceApi.kt` (`GET`/`POST /me/devices` reales), `feature/notifications/{NotificationsViewModel,NotificationsScreen}.kt` (botón "Enable notifications" real). Verificado con `NotificationsFlowTest` (instrumentado real, mismo dispositivo físico): token FCM real obtenido de Firebase, registrado contra el backend real, y confirmado por fuera del test consultando Postgres (`device_push_tokens`, token con el formato real `xxxx:APA91b...`). **Dos hallazgos reales durante esta verificación:** (1) `RegisterDeviceRequest.platform` tenía un valor por defecto (`"ANDROID"`); `kotlinx.serialization` omite del JSON cualquier campo cuyo valor actual coincida con su default declarado — sin importar si el llamador lo pasó explícitamente o no — así que `platform` desaparecía silenciosamente del body y la validación `@NotBlank` del backend lo rechazaba con 400; fix: quitar el default (el campo ahora es obligatorio en el constructor). (2) La primera corrida del test "pasó" con un falso positivo: una fila de prueba insertada manualmente vía `curl` durante el diagnóstico (con las credenciales de `testuser`, para reproducir el mismo contexto de autenticación) satisfacía la aserción laxa del test aunque el registro real seguía fallando con 400 — fix: aserción adicional de "no debe verse ningún error HTTP" además de la fila esperada. Sin cuenta de servicio de Firebase en este checkout, `FcmPushNotificationSender` del backend sigue sin activar — la entrega real de una notificación de punta a punta no se probó, solo el registro del token (tal como permite explícitamente la tarea). **Nota de alcance:** `GoogleService-Info-apple.plist` (iOS) también llegó durante esta sesión pero la integración de iOS se pospuso deliberadamente (ver `IOS-004`); el archivo permanece en la raíz del repo, sin mover ni usar. No hay configuración Firebase Web — `WEB-005` sigue `BLOCKED_ON_FIREBASE_CONFIG`. |
| AND-006 | Reporte de errores — Firebase Crashlytics (mismo proyecto Firebase que `AND-005`) | NFR-006 (observabilidad), local dev únicamente — no reemplaza la auditoría de seguridad pendiente | **DONE, verificado en dispositivo físico real con evidencia real y confirmación del usuario en Firebase Console** (2026-08-16) — plugin `com.google.firebase.crashlytics` (project + app-level `build.gradle.kts`) + `firebase-crashlytics-ktx` (mismo BoM `33.5.1` que `AND-005`). **Habilitado siempre en release; en debug queda apagado por defecto** — `VidaCotidianaApplication.onCreate()` llama `FirebaseCrashlytics.getInstance().setCrashlyticsCollectionEnabled(!BuildConfig.DEBUG \|\| BuildConfig.CRASHLYTICS_DEBUG_ENABLED)`, donde `CRASHLYTICS_DEBUG_ENABLED` es un `buildConfigField` leído de la propiedad Gradle `crashlyticsDebugEnabled` (default `"false"`, nunca hardcodeado `true`) — un build de debug de cualquier dev, sin pasar `-PcrashlyticsDebugEnabled=true` explícitamente, no reporta nada. Dos botones de debug en `RemindersScreen.kt` (`debug_record_exception_button`/`debug_crash_button`), gateados en runtime por el mismo flag, no ocultos — el objetivo es probar el pipeline real, no simularlo. **Verificación real, no asumida:** con `-PcrashlyticsDebugEnabled=true`, un tap real (`adb shell input tap`, sin arnés de test de por medio) sobre "Debug: crash" produjo un `FATAL EXCEPTION` real en `logcat` con el stack trace exacto (`RemindersScreen.kt:66`) y mató el proceso de verdad (`adb shell pidof` vacío después); el SDK de Crashlytics se inicializó y conectó de verdad a Firebase (`Initializing Firebase Crashlytics 19.2.1`, `Fetched settings` con un JSON de configuración real del proyecto, tokens de autenticación de Firebase Installations genuinos en los logs); tras relanzar la app, los archivos de reporte pendiente en `/data/data/com.vidacotidiana.app/files/.crashlytics.v3/` (confirmados vía `adb shell run-as`) ya no estaban — consistente con una subida exitosa, aunque Crashlytics no expone una API pública en tiempo real como GlitchTip para confirmarlo programáticamente. **El usuario confirmó visualmente en Firebase Console → Crashlytics que el evento llegó** — no se declaró visto sin esa confirmación explícita, tal como pedía la tarea. **Hallazgo real durante esta verificación:** el header de `RemindersScreen` con los 4 botones (Invitations/Notifications/Debug ×2) en un solo `Row` desbordaba el ancho de pantalla y los dos botones de debug ni siquiera aparecían en el árbol de UI Automator — mismo patrón de fallo que el bug de `ReminderRow` en `AND-004`, esta vez sin un título largo de por medio; fix: los botones de debug se movieron a su propio `Row`. También se encontró que `connectedAndroidTest` (Compose Testing) no observa el crash de la misma forma que un tap real vía `adb` — el test correspondiente (`CrashlyticsCrashTest`) pasa "en verde" sin que eso pruebe nada por sí solo, documentado explícitamente en su Javadoc; la evidencia real vino del tap manual, no del test instrumentado. `./gradlew assembleDebug` (flag por defecto, `false`) y `./gradlew assembleRelease`: ambos `BUILD SUCCESSFUL`. |
| AND-007 | Notificaciones locales para recordatorios propios (`ADR-007`/`FR-005`, gap real de V1 confirmado en `05-v2-plan.md`) | UC-03/`FR-005` | **DONE, verificado en dispositivo físico real — creación, disparo real, y supervivencia a un reinicio real** (2026-08-16) — Formulario de creación con selector de fecha/hora (`DatePicker`/`TimePicker` M3, `RemindersScreen.kt`), `CreateReminderRequest.dueAt` añadido (antes solo se podía crear un recordatorio con título — el gap de UI real que había que cerrar primero para que hubiera algo que programar). `core/notifications/`: `ReminderAlarmScheduler.kt` (`AlarmManager.setExactAndAllowWhileIdle`, no `WorkManager` — un recordatorio es un momento exacto, no una ventana aproximada), persistencia en `SharedPreferences` de lo programado (JSON vía kotlinx.serialization) porque `AlarmManager` **no sobrevive un reinicio** por diseño de Android — sin esa persistencia, `BootRescheduleReceiver` no tendría de dónde reprogramar. `LocalReminderNotifier.kt`: `NotificationChannel` propio (`local_reminders`), separado del canal que usa el push de `AND-005` — el usuario puede silenciar uno sin el otro. Permiso de alarmas exactas (`SCHEDULE_EXACT_ALARM`, API 31+) solicitado en runtime la primera vez que el usuario activa "Add due date" (`Settings.ACTION_REQUEST_SCHEDULE_EXACT_ALARM`), no asumido concedido. **Hallazgo real durante la verificación, con impacto retroactivo en `AND-005`:** la primera notificación programada nunca apareció pese a que la alarma disparó exactamente a tiempo (confirmado en `dumpsys alarm`) — `dumpsys notification` reveló `com.vidacotidiana.app importance=NONE`: `POST_NOTIFICATIONS` estaba en el manifest desde `AND-005` pero **nunca se solicitó en tiempo de ejecución** en ningún punto del código — en API 33+ eso significa que el permiso queda denegado para siempre sin que el usuario vea ningún diálogo. Corregido aquí (se solicita junto con el permiso de alarma exacta), pero el push de `AND-005` probablemente tiene el mismo gap sin cerrar — declarado explícitamente, no corregido en silencio dentro de esta tarea. **Verificación real, no simulada:** con el permiso ya concedido, un recordatorio con `dueAt` a ~1 minuto produjo una notificación real en la bandeja del dispositivo físico (`dumpsys notification` confirmó el `NotificationRecord` real, canal `local_reminders`, título "Reminder due"; captura de pantalla real de la bandeja de notificaciones). Reinicio real del dispositivo (`adb reboot`) con una alarma pendiente: tras el reinicio, `dumpsys alarm` confirmó la alarma reprogramada por `BootRescheduleReceiver` en el mismo instante exacto original (`origWhen=2026-08-16 17:10:00.000`, `exactAllowReason=permission`) — esto solo es posible si el receiver corrió de verdad, ya que `AlarmManager` limpia toda alarma al reiniciar. Cancelación al completar un recordatorio también verificada (`RemindersViewModel.completeReminder` llama `alarmScheduler.cancel`). `./gradlew clean assembleDebug assembleRelease`: `BUILD SUCCESSFUL`. `LoginAndRemindersFlowTest`/`SharingFlowTest`/`NotificationsFlowTest` (instrumentados reales, mismo dispositivo): en verde tras el cambio. |

| AND-008 | Enlace/botón real de registro ("Crear cuenta") desde el login, contra el `registrationAllowed: true` real de Task B §2 | `05-v2-plan.md`/`33-security-cross-audit.md` §1.2 | **DONE, verificado sin dispositivo físico (build + Custom Tab de AppAuth, verificación end-to-end real hecha desde Web — ver fila `WEB-008`, mismo flujo Keycloak, mismo `registrationServiceConfig`)** (2026-08-16/17) — `AuthManager.kt`: `buildRegisterIntent()` reutiliza `AuthorizationRequest`/`AuthorizationService.getAuthorizationRequestIntent` de AppAuth (PKCE/Custom Tab gratis), apuntando a `/protocol/openid-connect/registrations` en vez de `/auth` — mismo `tokenEndpoint`, así que el intercambio de código en `handleLoginResult` no necesitó ningún cambio. `LoginScreen.kt`: botón "Crear cuenta" (`testTag("register_button")`) junto al de login, reutilizando el mismo `launcher`/callback. `./gradlew compileDebugKotlin`/`assembleDebug`: éxito real. **CERRADO en dispositivo físico real, 2026-08-17.** Cuenta `devicetest_37282` creada de cero desde el botón "Crear cuenta" real de `LoginScreen.kt` en el teléfono físico (Custom Tab real → `register.ftl` real del tema mobile, con la marca "Vida Cotidiana" visible → formulario completado con datos reales) → tras enviar, la app aterrizó logueada directamente en `HomeScreen` como `devicetest_37282` — confirmado en Postgres (`SELECT * FROM users WHERE username='devicetest_37282'`, fila real). No se encontró ningún bug propio de `AND-008` en esta verificación (los 2 bugs reales de navegación encontrados en esta misma sesión de dispositivo, ver `UX-006` arriba, eran preexistentes al registro — afectaban a *cualquier* login, no algo introducido por el flujo de registro). |
| AND-009 | Renovación automática de sesión (Task B §3, `33-security-cross-audit.md` §1.3) | Hallazgo real de `AND-007`: 401 tras 5 min sin refresh, exigía login manual | **DONE, verificado sin dispositivo físico — mecanismo de refresh confirmado real contra el Keycloak en ejecución, verificación end-to-end completa (crear un recordatorio, esperar la expiración real, crear otro) pospuesta explícitamente al dispositivo físico** (2026-08-17) — `TokenStore.kt` reescrito para guardar el `AuthState` propio de AppAuth (`jsonSerializeString()`/`jsonDeserialize()`) en vez de 4 campos sueltos — sin ruta de migración, cambio limpio (nada de producción que preservar todavía). `AuthManager.getValidAccessToken(forceRefresh)`: usa `AuthState.performActionWithFreshTokens` (mecanismo documentado de AppAuth, no una llamada de refresh hecha a mano) — con `forceRefresh=true` fuerza `needsTokenRefresh=true` antes de invocarlo, porque el reloj del servidor es la autoridad, no el cálculo local de expiración de AppAuth. `TokenAuthenticator.kt` (nuevo, `okhttp3.Authenticator`, no un `Interceptor` — es el mecanismo idiomático de OkHttp para reintento-tras-401): en un 401 real, llama `getValidAccessToken(forceRefresh=true)` vía `runBlocking` (seguro: OkHttp ya ejecuta `Authenticator.authenticate` en su propio hilo, nunca el principal) y reintenta la request una sola vez (`responseCount(response) >= 2` corta el bucle) — si el `refresh_token` también expiró, cae de vuelta a exigir login manual, mismo comportamiento fail-closed que antes de esta tarea, ya no como primera respuesta a un token vencido. **Verificado sin el dispositivo del usuario, por instrucción explícita de esta tarea:** `./gradlew compileDebugKotlin`/`assembleDebug`: éxito real. El mecanismo servidor del que depende todo esto (`grant_type=refresh_token` contra el realm real) se confirmó por separado con una petición real (`curl`) al Keycloak en ejecución: un access token nuevo y distinto del original, emitido con éxito a partir del `refresh_token` real de `testuser`. **CERRADO en dispositivo físico real, 2026-08-17 — la verificación de punta a punta exacta que pedía la tarea, no una aproximación.** En el teléfono físico, cuenta `devicetest_37282`: recordatorio #1 creado a las 03:32:25 UTC (justo tras el login/registro, token recién emitido). Recordatorio #2 creado a las 03:38:43 UTC — **6 min 18 s después**, superando ampliamente los 300s de `accessTokenLifespan` — sin ningún prompt de login, sin error visible. Recordatorio #3 creado a las 03:40:21 UTC, misma sesión, sin re-login. Los 3 confirmados con timestamps reales vía `SELECT title, status, created_at FROM reminders JOIN users ...` contra Postgres — no son capturas de pantalla sin corroborar, son filas reales con marcas de tiempo reales que prueban el vencimiento real del token entre el #1 y el #2. Único camino de código posible para que el #2/#3 tuvieran éxito: `TokenAuthenticator` interceptó el 401 real del token vencido, `AuthManager.getValidAccessToken(forceRefresh=true)` ejecutó un `grant_type=refresh_token` real, y la request se reintentó con éxito — no hay ninguna otra ruta en el código que explique una creación silenciosa y exitosa pasado el vencimiento. Captura real del estado final (3 recordatorios, sin prompt de login) en `Documentacion/02-ux-ui/screenshots/android-token-refresh-verification.png`. |

## iOS (Swift / SwiftUI)

| ID | Tarea | Origen | Estado |
|---|---|---|---|
| IOS-001 | Bootstrap proyecto (SwiftUI, Swift Concurrency) | `08b-ios-architecture.md` | DONE (scaffold) / **BLOCKED_BY_ENVIRONMENT** (build real) (actualizado 2026-08-16) — `ios/` (SwiftUI, `Package.swift` con Swift Package Manager, ASSUMPTION per TBD del propio doc, `iOS 17` mínimo DEC-012), estructura `App/Core/{Network,Security,UI}`, `App/Feature/{Auth,Home,Reminders,Sharing,Settings}`, `App/Navigation` (paquetes vacíos, `.gitkeep`), una vista SwiftUI mínima. Cadena real de bloqueos, cada uno resuelto hasta el siguiente: (1) `/opt/homebrew` pertenecía a otro usuario — el usuario lo corrigió (`chown`). (2) `brew install xcodesorg/made/xcodes` falló compilando desde fuente (`xcbuild` requiere Xcode — circular); resuelto bajando el binario prebuilt de GitHub Releases (`xcodes 2.0.3`, universal) a `~/bin/xcodes`, sin depender de brew. (3) `xcodes install --latest` pedía login interactivo de Apple ID — el usuario lo corrió él mismo en su propia Terminal y confirmó instalación exitosa; verificado de verdad aquí con `xcodes installed` → `26.6 (17F113) [Apple Silicon] /Applications/Xcode-26.6.0.app`. (4) **Bloqueo actual:** activar ese Xcode (`sudo xcode-select -s ...`) y aceptar la licencia/`-runFirstLaunch` requieren de nuevo una contraseña de `sudo` interactiva que este entorno no puede proveer — confirmado con `xcodes select` fallando exactamente igual. Comandos exactos pendientes de que el usuario los corra él mismo, documentados en `ios/README.md`. |
| IOS-002 | Autenticación Keycloak (`ASWebAuthenticationSession` nativo, PKCE) | UC-01/UC-02 | **DONE, verificado en Simulador real** (2026-08-16) — `AuthManager.swift`: `ASWebAuthenticationSession` nativo (no AppAuth-iOS, más liviano), PKCE S256 (`PKCE.swift`, CryptoKit), cliente `ios-app` ya configurado en Keycloak, tokens en Keychain (`KeychainTokenStore.swift`). Verificado extremo a extremo con `LoginAndRemindersUITests` (XCUITest real, Simulador) contra Keycloak y backend reales: login real, redirect real, sesión persistida — confirmado además consultando Postgres directamente. **Dos bugs reales encontrados y corregidos:** (1) `session.start()` puede devolver `false` sin invocar el completion handler — dejaba la `continuation` colgada para siempre sin error visible; fix: verificar el booleano y lanzar `AuthError.sessionFailedToStart`. (2) `prefersEphemeralWebBrowserSession = false` esperaba una alerta nativa de consentimiento del sistema que nunca se resolvía en el contexto de test automatizado — SafariViewService se abría pero nunca navegaba; fix: `true` (además mejor para privacidad, no comparte cookies con Safari). El contenido de `ASWebAuthenticationSession` corre en un proceso separado (`com.apple.SafariViewService`, hallado con `launchctl list`) — análogo a Custom Tabs en Android, requiere su propio `XCUIApplication(bundleIdentifier:)`. Retest en iPad físico solicitado por la tarea quedó **inconcluso**: la corrida en background superó los 55 minutos sin producir resultado (vs. ~30s en Simulador) y se detuvo manualmente para no bloquear el resto del trabajo — no se declara ni pasado ni fallado, pendiente de reintento. |
| IOS-003 | Reminders: crear/listar/completar | UC-03/UC-04 | **DONE, verificado en Simulador real** (2026-08-16) — `ReminderAPI.swift` (URLSession) contra `GET/POST /reminders`, `POST /reminders/{id}/complete` reales; `RemindersView.swift` (SwiftUI). Verificado en el mismo `LoginAndRemindersUITests`: crea un recordatorio con sufijo aleatorio, confirma que aparece, lo completa, confirma `COMPLETED` — contra el backend real, confirmado también por fuera del test vía Postgres (`SELECT ... WHERE title LIKE 'XCUITest reminder%'`). `xcodebuild build`/`test` (Simulador): éxito real, sin simular. |
| IOS-004 | Sharing/Invitations UI | UC-07..UC-10, UC-14 | **TODO, deliberadamente pospuesto** (2026-08-16) — decisión explícita de la sesión: priorizar Android/Web tras encontrar varios bugs reales de plataforma cruzada (host/network-security-config, orden de la lista) que consumieron el tiempo disponible; retomar iOS en la siguiente sesión. |
| IOS-005 | Registro de push (FCM vía puente APNs) | UC-12 | TODO — bloqueado además por `BLOCKED_ON_FIREBASE_CONFIG` (sin `GoogleService-Info.plist` en este checkout) y por `IOS-004` pospuesto. |

## Web (React + TypeScript SPA)

| ID | Tarea | Origen | Estado |
|---|---|---|---|
| WEB-001 | Bootstrap proyecto (Vite/React/TS) | `08c-web-architecture.md` | DONE (2026-08-15) — `web/` (Vite + React + TypeScript, ASSUMPTION per TBD del propio doc; SPA, DEC-007/ADR-011), `browserslist` en `package.json` según DEC-013, estructura `src/core/{api,auth,ui}`, `src/features/{auth,home,reminders,sharing,settings}`, `src/routes` (paquetes vacíos, `.gitkeep`), una página mínima. Nota en `web/README.md` sobre el patrón de manejo de token pendiente para `WEB-002` (Opción 1 — memoria + renovación silenciosa, ya la más coherente con DEC-007/SPA, no una decisión nueva). `npm install` y `npm run build` ejecutados de verdad (Node disponible: `v25.8.0`/`npm 11.11.0`) — ambos con éxito real, no simulados. |
| WEB-002 | Autenticación Keycloak (OIDC/PKCE), patrón de almacenamiento de token (pendiente técnico ya documentado) | UC-01/UC-02, `11-auth-security.md` | DONE (2026-08-15) — `core/auth/{pkce,authClient,config}.ts`, PKCE S256 manual (sin librería, `crypto.subtle`), cliente `web-spa` (público). Token en memoria (variable de módulo, nunca `localStorage`/`sessionStorage`) + renovación silenciosa (`prompt=none` vía iframe oculto) — Opción 1 de `08c-web-architecture.md`, ya documentada. `sessionStorage` se usa solo para el `code_verifier` (estado efímero de un solo uso, no un token). Verificado extremo a extremo con un navegador real (Playwright/Chromium, headless): login completo contra Keycloak real, incluyendo un bug real encontrado y corregido (React StrictMode reintentaba el intercambio del authorization code — de un solo uso — y Keycloak correctamente rechazaba el segundo intento con 400; fix: guardia por `useRef` en `CallbackPage`). Requirió añadir CORS al backend (`SecurityConfig`, ver nota arriba) — hallazgo real, no anticipado en el contrato. |
| WEB-003 | Reminders: crear/listar/completar | UC-03/UC-04 | DONE (2026-08-15) — `features/reminders/{api,RemindersPage}.tsx` contra `GET/POST /reminders`, `POST /reminders/{id}/complete` reales. Verificado en el mismo test real de navegador: crear un reminder y completarlo, ambos reflejados de inmediato en la UI. `npm run build`: éxito real (`tsc -b && vite build`). |
| WEB-004 | Sharing/Invitations UI | UC-07..UC-10, UC-14 | **DONE** (2026-08-16) — `core/user/api.ts` (`GET /me`), `features/sharing/{api,ShareDialog,InvitationsPage}.tsx` (7 endpoints del contrato, formulario email/username, colaboradores + invitaciones pendientes con revocar/cancelar, `/invitations` con aceptar/rechazar). Verificado extremo a extremo con Playwright/Chromium real (`e2e/sharing.spec.ts`): login real contra Keycloak, crea un recordatorio, invita a un segundo usuario real (`userb`, provisionado vía password-grant + `GET /me` antes del test) por username — confirmado además por fuera del test consultando Postgres. El lado B (aceptar como `userb`) se verifica directamente contra el backend real (no se duplicó el login completo en el navegador para ambas cuentas), consistente con lo permitido por la tarea. `npm run build`: éxito real. |
| WEB-005 | Registro de push (Web Push), según alcance definido para V1 | UC-12 | **DONE, verificado con un token real y clic manual real** (actualizado 2026-08-16) — el usuario proveyó la config real de Firebase Web (proyecto `vida-cotidiana-6da30`, mismo proyecto que Android) inmediatamente después de cerrar `AND-005`. SDK `firebase` instalado, config en `web/.env` (git-ignorado; `web/.env.example` documenta las variables sin valores reales), `core/notifications/firebase.ts` (`requestWebPushToken()`), Service Worker (`public/firebase-messaging-sw.js`), `features/notifications/{api,NotificationsPage}.tsx` (`GET`/`POST /me/devices` reales) — ya no muestra ningún estado bloqueado. **Hallazgo real durante la verificación:** el registro automatizado vía Playwright (Chromium empaquetado y Chrome real, headless y headed) falla consistentemente en `pushManager.subscribe()` con `AbortError: Registration failed - permission denied` — el permiso de notificaciones se concede correctamente y el Service Worker se registra correctamente, pero el backend de push de Google rechaza la suscripción específicamente cuando el navegador es controlado por WebDriver (medida anti-abuso del lado de Google, no un bug de esta app, del SDK de Firebase, ni de la VAPID key). Confirmado pidiéndole al usuario un clic manual real (mouse, Chrome no automatizado): el dispositivo `WEB` se registró de verdad — confirmado por fuera consultando Postgres (`device_push_tokens`, `platform='WEB'`, token con el formato real `xxxx:APA91b...`, `2026-08-16 08:28:32`). `e2e/notifications.spec.ts` quedó ajustado para verificar automatizadamente todo lo que SÍ es alcanzable bajo WebDriver (login real, `GET /me/devices` real, botón habilitado) sin afirmar un resultado que Google bloquea estructuralmente para sesiones automatizadas — documentado explícitamente en el propio test, no ocultado. `npm run build`: éxito real. |
| WEB-006 | Reporte de errores — GlitchTip autoalojado (compatible con protocolo/SDK de Sentry) | NFR-006 (observabilidad), local dev únicamente — no reemplaza la auditoría de seguridad pendiente | **DONE, verificado con la propia API de GlitchTip (no solo la UI)** (2026-08-16) — ver `INFRA-007` para la infraestructura de GlitchTip en `docker-compose.yml`. `@sentry/react` instalado (GlitchTip habla el protocolo de ingesta de Sentry sin modificación — si algún día se migra a Sentry hosted u otro proveedor, solo cambia el DSN, no este código cliente). `core/errors/glitchtip.ts` (`initErrorTracking()`, `Sentry.init({ dsn })`), DSN vía `VITE_GLITCHTIP_DSN` (mismo patrón que `VITE_FIREBASE_*`: `.env` git-ignorado, `.env.example` documenta el nombre sin valor real — el DSN de GlitchTip/Sentry es público por diseño, igual que una API key de Firebase Web, pero se mantiene la misma separación limpia). `Sentry.ErrorBoundary` en `main.tsx` envolviendo `<BrowserRouter>`, con fallback visible (antes no existía ningún Error Boundary). Botón "Debug: trigger error" en `RemindersPage.tsx` — lanza durante el render (no dentro del handler de clic) para que el `ErrorBoundary` lo capture de verdad, ya que un Error Boundary de React nunca ve excepciones lanzadas desde un event handler. **Verificado extremo a extremo con Playwright real** (`e2e/error-tracking.spec.ts`): clic real en el botón, `ErrorBoundary` muestra el fallback, y el test consulta directamente la API de GlitchTip (`GET /api/0/projects/{org}/{project}/issues/`, compatible con la API de Sentry, con un `APIToken` real generado vía Django shell) — confirma programáticamente que el issue con el título exacto llegó, no solo mirando la UI. Confirmado además manualmente contra esa misma API fuera del test (`curl` con el token real): el issue aparece con `filename`/`function` correctos. `npm run build`: éxito real. |
| WEB-007 | Notificaciones locales para recordatorios propios (`ADR-007`/`FR-005`), homólogo Web de `AND-007` | UC-03/`FR-005` | **DONE, verificado con evidencia real de una `Notification` real del navegador, límite de plataforma documentado explícitamente** (2026-08-16) — `<input type="datetime-local">` en el formulario de creación (`RemindersPage.tsx`), `createReminder(title, dueAt)` ahora acepta fecha de vencimiento. `core/notifications/localReminderTimer.ts`: `setTimeout` real (nada de `Service Worker`/`periodicSync` — soporte demasiado inconsistente entre navegadores para prometerlo, ver `08c-web-architecture.md`), reutiliza el permiso de `Notification` ya solicitado por el flujo "Enable notifications" de `WEB-005` (nunca pide permiso por su cuenta). **Limitación real de plataforma, documentada explícitamente en `08c-web-architecture.md`, no ocultada ni forzada:** solo funciona mientras la pestaña esté abierta (incluso en segundo plano) — si la pestaña o el navegador se cierran, no hay temporizador de sistema operativo disponible para una página web sin ayuda del servidor. Asimetría real y deliberada frente a Android (`AlarmManager` sí sobrevive incluso a un reinicio). **Verificación real:** `e2e/local-notifications.spec.ts` — crea un recordatorio con `dueAt` ~2 minutos en el futuro, espera a que venza, y confirma que `new Notification("Reminder due", {body: title})` se invocó de verdad (capturado envolviendo el constructor real de `Notification` vía `page.addInitScript()`, no mockeado) — en verde. **Hallazgo real durante la escritura del test, no de la app:** un primer intento con un margen de 6 segundos falló — `datetime-local` solo tiene precisión de minuto (sin campo de segundos), así que el valor se trunca a su minuto; si "ahora" ya llevaba unos segundos corridos del minuto actual, ese truncamiento podía dejar el minuto elegido igual o anterior al momento real del envío, y `scheduleLocalReminder()` correctamente no programa nada con una demora no positiva — el recordatorio se creó bien (con la fecha correcta visible en la tarjeta), simplemente nunca se armó el temporizador. Fix: margen de prueba de 130s truncado a minuto (deja ≥70s reales), no un cambio en la app. `sharing.spec.ts`/`notifications.spec.ts`/`error-tracking.spec.ts` reverificados en verde tras el cambio. `npm run build`: éxito real. |
| WEB-008 | Enlace/botón real de registro ("Crear cuenta") desde el login, contra el `registrationAllowed: true` real de Task B §2 | `05-v2-plan.md`/`33-security-cross-audit.md` §1.2 | **DONE, verificado de punta a punta con una cuenta real creada de cero (no aprovisionada por admin), Playwright/Chrome real contra el backend/Keycloak reales** (2026-08-16/17) — `authClient.ts`: `register()` (mismo flujo PKCE que `login()`, apunta a `/protocol/openid-connect/registrations` en vez de `/auth` — mismo `redirect_uri`/cliente, así que `CallbackPage`'s intercambio de código no necesitó ningún cambio). Botón "Crear cuenta" en `LoginPage.tsx`. `register.ftl` añadido a ambos temas Keycloak (copia real extraída de `org.keycloak.keycloak-themes-25.0.6.jar` del contenedor en ejecución, mismo método que `login.ftl`/`template.ftl` de `UX-005`, mismo diff mínimo). **Verificación real:** cuenta `newuser_223zcs` creada desde el formulario de registro real → sesión iniciada automáticamente → fila real en Postgres (`SELECT * FROM users WHERE username='newuser_223zcs'`) → **login posterior** por separado con esa misma cuenta, también verificado en verde. **Segundo hallazgo real, corregido:** el título "Register" se partía a la mitad ("Regi"/"ster") por un bug real preexistente heredado del tema base (`word-break` de PatternFly encogiendo el título dentro de la columna flex angosta del split-panel Web) — nunca visible en el login porque "Sign in to your account" sí tiene espacios donde envolver; confirmado con `getComputedStyle`/`boundingBox()` real antes y después, corregido con una regla CSS mínima y escopeada en `login.css`. `npm run build`/`lint`: éxito real. Detalle completo, incluida una condición de carrera real encontrada en `UserSyncService` (no corregida, fuera de alcance) en `33-security-cross-audit.md` §1.2. |

## UX/UI (Documentacion/02-ux-ui/)

Gap real de la documentación original (CLAUDE.md pedía este directorio desde el inicio del proyecto y nunca se creó). Todo lo de esta sección es **RECOMMENDATION** de diseño, no requerimiento de negocio — ver la nota de estado al inicio de cada documento. `UX-001`..`UX-005`: pasada estrictamente visual sobre componentes ya funcionales (`AND-002`..`WEB-006`); ningún flujo, ruta, ni llamada de red cambió. `UX-006`: además de una pasada visual sobre las pantallas reales existentes (misma regla — sin tocar lógica), añade rutas/pantallas nuevas (Home con datos reales sobre endpoints ya existentes, y 6 módulos de solo-maqueta) — detalle de alcance en la fila `UX-006` de abajo.

| ID | Tarea | Origen | Estado |
|---|---|---|---|
| UX-001 | `design-system.md` — tokens de color, tipografía (Inter/Material 3 type scale), espaciado (grid 8pt), forma/elevación | NFR (consistencia visual), gap de `02-ux-ui/` nunca creado | **DONE** (2026-08-16) — paleta con un solo color semilla (`#4F46E5`, índigo) y roles derivados (success/warning/error + containers), roles tipográficos (`display`/`headline`/`title`/`body`/`label`), escala de espaciado de 8pt (`space-1`..`space-8`). **Hallazgo real durante la redacción, no al codificar:** los tonos base de `color-success`/`color-warning` (600) fallan el umbral AA de 4.5:1 para texto normal sobre blanco (3.77:1 y 3.19:1 respectivamente, calculado con la fórmula de contraste WCAG real, no estimado) — se añadieron variantes `-text` (tono 700) específicas para texto, dejando el tono base solo para ícono/fondo/borde (umbral de 3:1). Sin este hallazgo, el token tal como se iba a documentar originalmente habría sido inaccesible por diseño. |
| UX-002 | `ux-principles.md` + `accessibility.md` — principios de UX, contraste AA, touch targets 48dp/44px, soporte de lector de pantalla | NFR (accesibilidad) | **DONE** (2026-08-16) — tabla completa de contraste con los 10 pares color/fondo relevantes y su ratio exacto (no solo los que pasan). Touch targets: 48dp (Android, guía Material/Android Accessibility) vs. 44px (Web, WCAG 2.5.5 AA) — declarado explícitamente como una diferencia real de guía por plataforma, no un error de transcripción. Requisitos de `contentDescription`/`liveRegion` (Android, TalkBack) y `role="alert"`/`aria-label`/`:focus-visible` (Web) aplicados en el código de `UX-004`, no solo documentados en abstracto. Declarado explícitamente **TBD/FUTURE** lo que NO se cubrió: auditoría automatizada (axe/Lighthouse) y pruebas manuales reales con lector de pantalla activado — ninguna corrida real de ninguna de las dos, no se simula. |
| UX-003 | `platform-guidelines.md` — qué se comparte (tokens) vs. qué usa convención nativa (FAB Android vs. botón fijo Web, etc.) | `ux-principles.md` §5 "paridad funcional, no pixel-por-pixel" | **DONE** (2026-08-16) — documenta explícitamente qué NO cambió en esta pasada (estructura de navegación, `NavigationBar` de Material 3 para Android, indicador de carga tipo spinner en Web) como candidatos a una iteración de UX futura, no como algo hecho ahora. |
| UX-004 | Aplicación de los tokens de `UX-001` a los componentes existentes — Android y Web, en el orden: (1) tarjeta de recordatorio, (2) espaciado 8pt en toda pantalla, (3) estados vacío/carga/error, (4) diálogo de compartir — más login/notifications/invitations | UX-001..UX-003 | **DONE, verificado con captura real antes/después en ambas plataformas** (2026-08-16) — **Android:** `core/ui/{Color,Type,Spacing,Theme}.kt` nuevos, `VidaCotidianaTheme` reemplaza el `MaterialTheme` sin personalizar que tenía la app desde `AND-001`; `RemindersScreen`/`ShareDialog`/`LoginScreen`/`NotificationsScreen`/`InvitationsScreen` restilizados — tarjeta de recordatorio con `Card` + badge de estado (texto + color, nunca solo color) + botones en fila propia (nunca comprimidos), estado vacío nuevo ("No reminders yet"), error con `liveRegion`. Capturas reales en `Documentacion/02-ux-ui/screenshots/android-*-{before,after}.png` (dispositivo físico real, Samsung Galaxy) — incluye el diálogo de compartir con colaboradores/invitaciones pendientes renderizado con datos reales. Verificado funcionalmente con `SharingFlowTest` (instrumentado real, mismo dispositivo) tras el restilo — sigue en verde, confirmado además por fuera consultando Postgres. **Web:** tokens como variables CSS en `index.css` (reemplazando por completo el CSS residual del template de Vite — nunca se había tocado desde `WEB-001`), fuente Inter autoalojada (`@fontsource/inter`, no una llamada a Google Fonts CDN). `RemindersPage`/`ShareDialog`/`InvitationsPage`/`NotificationsPage`/`LoginPage` con CSS Modules por componente. Capturas reales en `Documentacion/02-ux-ui/screenshots/web-*-{before,after}.png` (Playwright, Chrome real). Verificado funcionalmente con `sharing.spec.ts`/`notifications.spec.ts`/`error-tracking.spec.ts` (Playwright real) tras el restilo — los 3 en verde. **Hallazgo real durante la verificación (Android):** el mismo patrón de bug que ya había aparecido en `AND-004`/`AND-006` (una `Row` sin límites de ancho comprimiendo/descartando hijos) — evitado esta vez de raíz reestructurando la tarjeta en dos filas (título+badge arriba, acciones abajo) en vez de parchear con un ancho fijo. **Segundo hallazgo real:** separar el título y el badge de estado en dos nodos de texto distintos (antes eran un solo `Text("$title — $status")`) rompió la aserción de `LoginAndRemindersFlowTest` que buscaba ese string concatenado exacto (`"$title — COMPLETED"`) — no un bug de la app (el usuario sigue viendo título y estado con más claridad que antes), sino un ajuste de test necesario tras un cambio real de estructura visual; corregido escribiendo la aserción contra el badge ("Completed", texto de título, no `COMPLETED`) escopeado por el testTag de la fila. **Nota de sesión, no relacionada con el código:** durante la reverificación final, los cuatro tests instrumentados empezaron a fallar de forma consistente con la actividad principal destruyéndose sola ~2s después de abrir (coincidiendo con una `EmptyActivity` interna de AndroidX Test) — se investigó exhaustivamente (permisos, batería/doze, reinstalación limpia) antes de concluir que era agotamiento de estado del dispositivo físico tras muchas horas continuas de pruebas en la misma sesión; un reinicio real del teléfono lo resolvió de inmediato y los 4 tests volvieron a pasar en verde — documentado aquí para que quede claro que no se ocultó ni se ignoró una señal de fallo real antes de descartarla como ambiental. `./gradlew assembleDebug`/`assembleRelease` y `npm run build`: ambos con éxito real; los 4 tests instrumentados de Android (`LoginAndRemindersFlowTest`, `SharingFlowTest`, `NotificationsFlowTest`, `CrashlyticsRecordExceptionTest`) y los 3 de Web (`sharing`/`notifications`/`error-tracking.spec.ts`) en verde tras el restilo completo. |
| UX-005 | `login-theme.md` + tema visual de Keycloak por cliente (mobile split panel single-column vs. web split panel), `infra/keycloak/themes/vida-cotidiana-{mobile,web}/` | Continuación de `UX-001`/`UX-004` — el login es la única pantalla del flujo real que quedó sin tocar por vivir fuera del código de la app | **DONE, verificado con captura real antes/después en ambas plataformas** (2026-08-16) — dos temas Keycloak estándar (`parent=keycloak.v2`, sin plugin), construidos a partir de una copia real de `login.ftl`/`template.ftl` extraída del propio `quay.io/keycloak/keycloak:25.0` en ejecución (no escritos de memoria), con el mínimo diff posible: una línea de marca en `login.ftl` (ambos temas) y un `<aside>` decorativo (`aria-hidden`) en `template.ftl` (solo web, para el panel dividido). `attributes.login_theme` por cliente: `android-app`/`ios-app` → `vida-cotidiana-mobile`, `web-spa` → `vida-cotidiana-web`. **Hallazgo real #1 (Admin REST API):** el primer intento asumió un campo `loginTheme` de nivel superior en `ClientRepresentation` (visto en documentación de un provider de Terraform/Pulumi) — la propia API de Keycloak en ejecución lo rechazó (`Unrecognized field "loginTheme"`), revelando que el campo real es `attributes.login_theme`; corregido tanto en la llamada admin como en `realm-vida-cotidiana.json`. **Hallazgo real #2 (CSS):** el panel dividido de Web, construido con `grid-template-columns: min(45vw,480px) 1fr`, no cuadraba (`getComputedStyle` mostraba `480px 700px` en vez de `480px 960px`) — el tema base `keycloak.v2` trae su propio `column-gap` de 64px y padding en `.pf-v5-c-login__container` para un grid responsive interno que no usamos, sin `box-sizing:border-box`; confirmado con `getComputedStyle`/`boundingBox()` real (no reinspección visual) antes de corregir con un reset explícito. **Verificación:** ambos temas devuelven `200` reales contra el Keycloak en ejecución, sin errores de FreeMarker. Android: login real por Custom Tab en dispositivo físico (capturas `screenshots/android-kc-login-{before,after}.png`, tema desactivado temporalmente vía Admin API para la captura "before" y restaurado después); `LoginAndRemindersFlowTest`/`SharingFlowTest` (instrumentados reales) en verde contra la página con tema. Web: capturas reales `screenshots/web-kc-login-{before,after}.png` (Playwright/Chrome real, misma técnica de desactivar/restaurar); `sharing.spec.ts`/`notifications.spec.ts`/`error-tracking.spec.ts` (cada uno inicia sesión por esta página real) en verde contra la página con tema. El contenedor de desarrollo (`vc-dev-keycloak`) se parcheó en caliente (`docker cp` + Admin API) en vez de recrearse desde el realm JSON actualizado, para no perder `testuser`/`userb` (usuarios reales creados fuera del JSON, referenciados por columnas `owner` en Postgres) — el JSON versionado queda consistente para una importación limpia futura. `realm-vida-cotidiana-test.json`: sin cambios (no tiene clientes `android-app`/`ios-app`/`web-spa`, solo se usa para tests de validación de tokens del backend). iOS: `attributes.login_theme` ya configurado en el realm (`vida-cotidiana-mobile`), pero no verificado en dispositivo — **FUTURE**, ver `login-theme.md` §9. **Actualización posterior, misma fecha:** se añadió un ornamento SVG inline abstracto (círculos superpuestos, sin significado icónico) al panel hero de Web — **explícitamente no un logo/marca** (ninguno ha sido aprobado en `02-ux-ui/`, inventarlo sería una decisión de producto no autorizada). Sin imagen de referencia disponible para replicar fielmente en este entorno — declarado como limitación explícita en `login-theme.md` §8, no forzado. Mobile se mantiene intencionalmente sin ornamento (viewport angosto). Recapturado `web-kc-login-after.png` y re-verificado `sharing`/`notifications`/`error-tracking.spec.ts` en verde tras el cambio. |

| UX-006 | Rediseño dashboard (Android + Web) a partir de `docs/Ejemplo APK vida Cotidiana.png`/`docs/Ejemplo Web vida Cotidiana .png` — tema oscuro real (`design-system.md` §1 corrección), librería de componentes (`design-system.md` §7), Home con datos reales, 6 módulos de solo-maqueta (Documentos/Inventario/Garantías/Mantenimiento/Suscripciones/Familia) | Imágenes de referencia del usuario, continuación de `UX-001`..`UX-005` | **DONE, verificado sin depender del dispositivo físico del usuario (build real + e2e reales contra backend/Keycloak/GlitchTip en ejecución), por instrucción explícita de la tarea** (2026-08-16) — **Alcance (Sección 1 de la tarea):** módulos reales con restilo puro (sin tocar lógica) — Tareas (`RemindersScreen.kt`/`RemindersPage.tsx`), Compartidos (`InvitationsScreen.kt`/`ShareDialog.kt`/`InvitationsPage.tsx`/`ShareDialog.tsx`), Notificaciones (`NotificationsScreen.kt`/`NotificationsPage.tsx`). 6 módulos nuevos de solo-maqueta — UI+ViewModel/hook con `MockData`, cero endpoints, cero migraciones Flyway, cero lógica de negocio: `feature/{documents,inventory,warranties,maintenance,subscriptions,family}` (Android) y `features/{documents,inventory,warranties,maintenance,subscriptions,family}` (Web). **Finanzas e "IA Asistente" — presentes en la imagen de referencia — completamente ausentes: ninguna pantalla, ruta, ítem de menú ni "Próximamente"**, por restricción explícita de `CLAUDE.md`. Detalle de decisiones de alcance (Compartidos fuera del sidebar de la referencia, Configuración omitida, búsqueda Web decorativa, landing post-login distinta por plataforma) documentado en `design-system.md` §7, no repetido aquí. **Android:** `core/ui/{Color,Theme}.kt` extendidos con rol `info` y `VidaColors`/`LocalVidaColors`/`VidaTheme.colors` (`CompositionLocal`) — corrigiendo en el camino un bug real de modo oscuro preexistente desde `UX-004` en 4 pantallas que leían `VidaColor.*Light` directamente (detalle completo en `design-system.md` §1). Librería nueva en `core/ui/components/` (8 archivos: `MetricCard`, `ListSectionCard`, `ListItemRow`, `StatusPill`, `BadgeTone`, `VidaFilterChip`, `DonutChart`, `AppTopBar`, `VidaBottomNav`). `NavGraph.kt` reescrito: `Scaffold` con barra inferior condicional (oculta en pantallas empujadas), `HomeScreen`/`HomeViewModel` con datos reales (`GET /reminders`, `GET /me/invitations`, `GET /me` — conteos pendiente/completado/vencido, próximas tareas, donut real completado-vs-pendiente), `MoreScreen` como hub para Notificaciones + los 6 módulos + cerrar sesión. Nueva dependencia `androidx.compose.material:material-icons-extended` (justificada: la app no tenía ninguna librería de íconos hasta ahora). **Web:** `index.css` extendido con `--color-info*`/`.badge-error`/`.badge-primary`, tope de ancho global de 640px removido (necesitaba ancho completo para el layout de sidebar). Librería nueva en `core/ui/components/` (`MetricCard`, `ListSectionCard`, `ListItemRow`, `DonutChart`, `FilterChip`) + `core/ui/icons.tsx` (12 íconos SVG hechos a mano — sin añadir una librería de íconos, mismo criterio de "no agregues una dependencia sin justificarla" ya aplicado en Android para `material-icons-extended`, aquí no había justificación suficiente). `core/ui/layout/AppShell.tsx` (sidebar de 232px + encabezado, colapsa a franja horizontal <900px), `features/home/{HomePage,useHomeData}` con la misma regla de datos reales que Android. `AppRouter.tsx` con las 10 rutas nuevas envueltas en `RequireAuth`. **Verificación real, sin dispositivo físico (Android) — por instrucción explícita del usuario para esta tarea:** `./gradlew compileDebugKotlin`/`./gradlew assembleDebug`: ambos con éxito real, cero warnings tras corregir un `Icons.Filled.Logout` deprecado. **No se corrieron los tests instrumentados** (`LoginAndRemindersFlowTest`/`SharingFlowTest`/`NotificationsFlowTest`) — requieren el dispositivo físico del usuario, explícitamente pospuesto hasta que el usuario indique proceder; el restilo de `RemindersScreen.kt`/`InvitationsScreen.kt`/`ShareDialog.kt`/`NotificationsScreen.kt` preservó cada `testTag`/texto exacto del que dependen esos tests (`reminder_row_*`, `share_button_*`, `notifications_button`, badges "Completed"/"Pending", etc.) — verificado por inspección de código, no por corrida real, y así se declara. **Verificación real, Web (con backend/Keycloak/GlitchTip reales en ejecución, sin depender de ningún dispositivo del usuario — un navegador Chrome real en esta misma máquina, la misma técnica que ya usan `WEB-002`..`WEB-007`):** `npm run build`/`npm run lint`: éxito real. **Los 4 e2e reales existentes corridos de nuevo tras el restilo completo — los 4 en verde, no solo compiló:** `sharing.spec.ts`, `notifications.spec.ts`, `local-notifications.spec.ts`, `error-tracking.spec.ts` (`npx playwright test`, Chrome real, contra el backend/Keycloak/GlitchTip reales ya en ejecución en esta máquina). Capturas reales (Playwright/Chrome, login real) en `Documentacion/02-ux-ui/screenshots/web-dashboard-{home,home-dark,documents,inventory,warranties,warranties-dark,reminders}-after.png` — incluye modo oscuro real, no solo claro. **CERRADO en dispositivo físico real, 2026-08-17 (el usuario conectó el teléfono y pidió continuar la verificación pospuesta arriba):** `adb devices` confirmó el Samsung físico real conectado; alcanzable por LAN al backend/Keycloak del Mac (`192.168.0.18`). **Dos bugs reales encontrados solo por probar en el dispositivo físico — ninguno visible en build/lint/e2e de escritorio — corregidos, no ocultados:**
  1. **Regresión real de navegación:** `onLoggedIn` (introducido por `UX-006`) navegaba a `Routes.HOME` tras el login — pero los 3 tests instrumentados reales dependen de que `reminder_title_input`/`add_reminder_button` estén alcanzables inmediatamente después de loguearse (mismo patrón que ya se decidió para Web, ver `design-system.md` §7: "Tareas es solo una pestaña más, no un peor punto de aterrizaje que Inicio"). Corregido: `onLoggedIn` navega a `Routes.REMINDERS`.
  2. **Bug real de Navigation-Compose, más sutil, encontrado después de corregir el #1 y que el test siguiera fallando igual:** `val startDestination = if (authManager.isLoggedIn()) Routes.HOME else Routes.LOGIN` en `AppNavGraph` se recalculaba en **cada recomposición** — incluida la que dispara `currentBackStackEntryAsState()` en cualquier `navigate()`, incluido el propio `navigate(Routes.REMINDERS)` del punto anterior. Para cuando esa recomposición ocurría, `isLoggedIn()` ya era `true`, así que `startDestination` se recalculaba a `Routes.HOME` y `NavHost` reseteaba el back stack ahí — silenciosamente pisando la navegación real que se acababa de pedir. Diagnosticado comparando una corrida limpia (recién reinstalada, sesión realmente vacía) contra el volcado real de semántica (`printToLog`) del árbol de Compose en el momento exacto del fallo — mostraba `HomeScreen` con el saludo real de `testuser`, no `RemindersScreen`. Corregido: `remember { ... }` alrededor del cálculo, para que se evalúe una sola vez.
  3. **Efecto colateral real del propio proceso de verificación, no del código:** las pruebas manuales previas (registro real vía `AND-008`, ver más abajo) dejaron una sesión SSO de Keycloak viva en el Chrome/Custom Tab del dispositivo — el botón "Log in" empezó a reautenticar en silencio contra esa cuenta anterior en vez de mostrar el formulario real. Resuelto limpiando los datos de Chrome (`pm clear com.android.chrome`) antes de continuar — declarado aquí porque también es una lección real para cualquier verificación futura en este mismo dispositivo, no algo que haya que repetir a ciegas.

Con ambos bugs corregidos: **`LoginAndRemindersFlowTest`, `SharingFlowTest` y `NotificationsFlowTest` — los 3 en verde, cada uno corrido de forma independiente** (`./gradlew connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=...`, consistente con cómo este proyecto siempre corrió estos tests — no hay `clearPackageData` configurado entre tests de una misma corrida por lotes, así que correrlos in batch falla por diseño en el 2º test en adelante, no es un bug nuevo). Capturas reales del dispositivo físico en `Documentacion/02-ux-ui/screenshots/android-dashboard-{home,reminders,more,warranties}-after.png` — confirman visualmente el restilo completo (íconos, `StatusPill`, `MetricCard`, sidebar/bottom-nav, badges de mock modules) tal como se ve en el hardware real, no solo en el build. |

## Infraestructura

| ID | Tarea | Origen | Estado |
|---|---|---|---|
| INFRA-001 | `docker-compose.yml` local (Postgres + Keycloak) | `18-dev-environment.md` | DONE |
| INFRA-002 | Exportar/versionar realm de Keycloak (`vida-cotidiana`) para bootstrap reproducible | `11-auth-security.md` | DONE (2026-08-15) — `infra/keycloak/realm-vida-cotidiana.json` y `realm-vida-cotidiana-test.json`, exportados de verdad vía la API de administración real de un Keycloak 25 levantado con Docker (no escritos a mano): realm creado vía `POST /admin/realms`, exportado vía `POST /admin/realms/{realm}/partial-export`. Nombres exactos ya fijados en `application.yml`/`application-test.yml` (`vida-cotidiana`, `vida-cotidiana-test`). `docker-compose.yml` actualizado a `start-dev --import-realm` + volumen `./infra/keycloak:/opt/keycloak/data/import`; ciclo import→realms activos verificado de verdad con un contenedor Keycloak real (issuers `http://localhost:.../realms/vida-cotidiana{,-test}` sirviendo correctamente). Corregido también el comentario obsoleto de la línea 1 de `docker-compose.yml` (aún mencionaba AWS/DEC-008 pre-`ADR-014`). **Actualización 2026-08-15 (misma fecha, sesión de AND-002/IOS-002/WEB-002):** 3 clientes públicos con PKCE S256 añadidos al realm dev — `android-app`/`ios-app` (redirect `com.vidacotidiana.app://callback`), `web-spa` (redirect `http://localhost:5173/callback`). Verificados con un flujo Authorization Code + PKCE real (script headless) contra Keycloak antes de escribir código de cliente alguno; el mismo `web-spa` fue luego confirmado de nuevo end-to-end desde un navegador real (WEB-002). |
| INFRA-003 | CI (lint, tests, SAST, dependency scan, secret scan, build) | `19-cicd.md` | DONE (2026-08-15) — `.github/workflows/backend-ci.yml` (checkout → JDK 21 → compile → test → SpotBugs → gitleaks → package → upload artifact), `.github/dependabot.yml` (Maven + github-actions, semanal). **ASSUMPTION, no DECISION:** GitHub Actions, porque GitHub es el único nombre relacionado con CI en toda la documentación (`25-open-questions.md` pregunta 4, todavía abierta) — ver nota ASSUMPTION en `19-cicd.md`. Sin deploy a staging (TBD, ya documentado). Branch protection anotado como paso manual pendiente en `19-cicd.md` (config del repositorio remoto, no versionable). |
| INFRA-004 | Entorno `staging` en servidor propio alquilado (Docker Compose o equivalente, sin servicios gestionados de AWS) | `19-cicd.md`, DEC-008/ADR-014 | TODO |
| INFRA-005 | Adapter de correo — proveedor `TBD` (DEC-009 reabierta, ver ADR-014); implementación actual: no-op/log-only | DEC-009 | DONE (adapter no-op) — ver `BE-017` |
| INFRA-006 | `/actuator/health` real, expuesto y correctamente configurado (sin CI/Crashlytics/auditoría de seguridad — eso viene después) | `07-backend-architecture.md` "Observabilidad — healthcheck", NFR-006 | **DONE, verificado con evidencia real de los casos UP y DOWN** (2026-08-16) — `spring-boot-starter-actuator` ya estaba en `pom.xml`; se confirmó/ajustó `application.yml`: `management.endpoints.web.exposure.include=health` (nada más — ningún otro endpoint de Actuator expuesto, superficie de ataque mínima) y `management.endpoint.health.show-details=when-authorized` (estaba en `never`, se cambió — un caller sin autenticar no debe ver el desglose por componente). `SecurityConfig` ya tenía la excepción `permitAll()` para `/actuator/health` en la cadena del resource server OAuth2 (previamente añadida, ahora confirmada real). **Evidencia real, no asumida:** `curl` sin header `Authorization` → `200 {"status":"UP"}` con Postgres arriba; apagando `vc-dev-postgres` de verdad (`docker stop`) → `curl` sin auth → `503 {"status":"DOWN"}` real (el `DataSourceHealthIndicator` de Spring Boot, gratis con `spring-boot-starter-data-jpa` + driver de Postgres, detectó la caída sin código adicional); reencendiendo el contenedor (`docker start`) → vuelve a `200 {"status":"UP"}` sin reiniciar el backend (HikariCP se recupera solo). Con un bearer token real, el desglose por componente sí aparece (`db`/`diskSpace`/`ping`), confirmando que `show-details=when-authorized` funciona en ambos sentidos. Test de integración real: `HealthCheckIntegrationTest` (Testcontainers) cubre el caso UP sin autenticar (200, `status=UP`, sin `components` en el body) — 2/2 en verde. El caso DOWN se dejó **documentado con evidencia real de `docker stop`/`docker start` en vez de automatizado**: apagar el contenedor Postgres de clase compartido por Testcontainers a mitad de un test afectaría a cualquier otra clase de test que lo reutilice en la misma JVM, así que se verificó contra el stack de desarrollo real en vez de forzarlo dentro del arnés de tests (justificado en el Javadoc de la clase). **Hallazgo real, fuera de alcance de esta tarea (declarado, no corregido):** una petición autenticada (con bearer token válido) a `/actuator/health` durante la caída de Postgres devuelve `500 INTERNAL_ERROR` en vez de reflejar el estado `DOWN`, porque `UserSyncFilter` corre igual sobre cualquier request con token — incluyendo paths `permitAll()` — y su upsert de usuario falla contra una base de datos caída antes de llegar al endpoint de salud. El caso sin autenticar (el que realmente usaría un healthcheck de `docker-compose` o un balanceador) funciona correctamente; el caso autenticado queda anotado para una futura revisión, no corregido aquí por instrucción explícita de mantener el alcance acotado. `./mvnw clean test`: 110/110 en verde (108 heredados + 2 nuevos de `HealthCheckIntegrationTest`). |
| INFRA-007 | GlitchTip autoalojado (`docker-compose.yml`), infraestructura de desarrollo local para `WEB-006` | 18-dev-environment.md, NFR-006 | **DONE, stack real levantado y verificado** (2026-08-16) — reutiliza el servicio `postgres` ya existente (base de datos `glitchtip` separada, creada por `infra/postgres/init-glitchtip-db.sql` vía `docker-entrypoint-initdb.d`, no un segundo contenedor de Postgres) + `glitchtip-redis` (Redis, requisito propio de GlitchTip para su cola de tareas Celery — no una adición nuestra) + `glitchtip-migrate` (init, corre `manage.py migrate` una vez) + `glitchtip-web`/`glitchtip-worker` (imagen oficial `glitchtip/glitchtip:v4.1.3`). Mismo comentario de "infraestructura de desarrollo local, no producción" que ya tenía Keycloak — migra a `INFRA-004` cuando exista el servidor propio. **Dos hallazgos reales de entorno durante la verificación (no bugs de la app, del sistema operativo/Docker de esta máquina):** (1) el puerto host `5432` ya estaba ocupado por una instalación nativa de PostgreSQL 17 (`/Library/PostgreSQL/17/bin/postgres`, ajena a Docker) — remapeado a `5434:5432` en `docker-compose.yml`, sin afectar la comunicación contenedor-a-contenedor (`postgres:5432` interno sigue igual). (2) Docker Desktop en macOS rechazó el bind mount del script de init con "operation not permitted" — restricción TCC de macOS específica de la carpeta `~/Documents`, no relacionada con el compose file; resuelto por el usuario concediendo acceso a "Carpeta Documentos" a Docker Desktop en Ajustes del Sistema → Privacidad y Seguridad (paso único, documentado en el comentario del propio `docker-compose.yml` junto con el workaround vía `docker exec ... psql -c "CREATE DATABASE glitchtip"` si alguna vez se repite). **Verificado real:** `docker compose up -d postgres glitchtip-redis glitchtip-migrate glitchtip-web glitchtip-worker` — los 5 contenedores llegaron a `Up`/`healthy`; `curl http://localhost:8000/_health/` → `200`; organización/proyecto/API token reales creados vía `manage.py shell` (`vida-cotidiana`/`vida-cotidiana-web`); el token real se usó desde `web/e2e/error-tracking.spec.ts` (ver `WEB-006`) para confirmar que un evento real llegó a la API — no solo que el contenedor responde. |

## No hacer todavía (explícito, ver `26-v1-backlog.md` y reglas de la fase de kickoff)

Sharing/push/Android/iOS/Web no se inician hasta estabilizar el vertical slice base (BE-001..013) con build/tests reales en verde en un entorno con JDK 21 + Docker — ver `02-validation-report.md`. **Actualización 2026-08-15:** esa condición ya se cumplió (108/108 tests reales); `AND-001`/`IOS-001`/`WEB-001` (bootstrap únicamente) se ejecutaron en esta misma actualización. `AND-002`/`IOS-002`/`WEB-002` en adelante (auth real, consumo de API) siguen `TODO`.
