# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local env (see .env.example for the variable names this project expects)
.env
.env.local

# Playwright
test-results
playwright-report
playwright/.cache
# e2e/calendar.spec.ts writes ad-hoc verification screenshots here — the
# canonical, committed copies live in Documentacion/02-ux-ui/screenshots/
# (same convention as every other UX-* screenshot).
e2e/screenshots

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
