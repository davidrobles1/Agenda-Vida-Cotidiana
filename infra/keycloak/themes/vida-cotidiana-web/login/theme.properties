# UX-005/Documentacion/02-ux-ui/login-theme.md — same mechanism as
# vida-cotidiana-mobile (see that theme's theme.properties for the full
# rationale). This theme additionally overrides template.ftl to add the
# split-panel hero — see that file's own comment for why.
parent=keycloak.v2

styles=css/styles.css css/login.css

kcBodyClass=vc-theme vc-theme-web
